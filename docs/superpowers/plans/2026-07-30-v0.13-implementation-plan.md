# v0.13 Desktop 拡張（`.mcpb`）実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 受講者がターミナルを一切開かずに Claude Desktop から aiftp を導入・設定・本番デプロイ・ロールバックできる `.mcpb` パッケージを出荷する。

**Architecture:** Desktop 用の別系統は作らない。`user_config.local_root` を「本物のプロジェクトフォルダ」に仕立てる冪等ブートストラップ（`packages/core/src/bootstrap/`）を起動時に走らせ、既存の cwd 中心 MCP サーバをそのフォルダで起動する。これにより既存 25 ツールが無改変で動く。新規パッケージ `packages/desktop-ext`（npm 非公開）が manifest 生成・依存同梱・`mcpb pack` を担う。

**Tech Stack:** TypeScript / Node.js 22+ / pnpm workspace / vitest / biome / `@modelcontextprotocol/sdk` 1.29 / zod 4 / `@anthropic-ai/mcpb`

**前提 spec:** [`2026-07-30-v0.13-desktop-extension-design.md`](../specs/2026-07-30-v0.13-desktop-extension-design.md)

---

## 仕様からのデルタ（実コード調査で判明。計画側で吸収済み）

| # | spec の記述 | 実態 | 本計画での扱い |
|---|---|---|---|
| D1 | 「既存 `init` のテンプレート生成ロジックを再利用」（§3.2） | `renderConfig` は `packages/cli/src/index.ts:357` の**非エクスポート関数**で、`TemplateConfig` / `InheritedSections` に依存する約 100 行。core は cli に依存できない（依存方向が逆） | `renderConfig` は**移設しない**。core が既に公開している `appendProfileBlock()`（`config-edit.ts:140`）でプロファイルブロックを生成する。これは Desktop に必要な全フィールドをちょうど持つ |
| D2 | 「`.gitignore` 追加も既存 `init` と同じ処理を通す」（§4.4） | `ensureGitignore` も CLI 非エクスポート（`index.ts:424`、8 行）。**git リポジトリ判定は一切していない**（`.gitignore` が無ければ新規作成する） | Task 1 で core へ移設。spec §4.4 の「git リポジトリでないフォルダではスキップ」を**新規に実装**する（現行 CLI の挙動は変えず、core 関数のオプションで切り替える） |
| D3 | Keychain サービス名 `aiftp:<site>-<profile>` | 生成関数 `buildKeychainServiceInitial` は `packages/cli/src/init-flow.ts:303` にある（core には無い） | Task 1 で core へ移設し、cli は re-export |
| D4 | `aiftp_profile_test` が接続確認に使える | `runtime.runDoctor` 未注入だと**全呼び出しを拒否**する（`mcp/src/index.ts:1417`）。注入しているのは CLI の `defaultStartMcp` のみ | Desktop の server entry は `createCli({ cwd }).parseAsync([...,'mcp'])` を呼ぶ。CLI 側の変更ゼロで `runDoctor` 注入込みの起動を再利用する |
| D5 | MCP prompt を追加する（§5.2） | 現行コードに `registerPrompt` は **1 件も無い**（tools 25 + resources 3 のみ） | Task 6 で新規導入。SDK 1.29 の `registerPrompt(name, {title?, description?, argsSchema?}, cb)` を使う |
| D6 | 「CI（release トリガー）で `.mcpb` をビルドし Release へ自動添付」（§6.5） | **release ワークフローは存在しない**（`ci.yml` / `smoke.yml` の 2 本のみ、両方 `permissions: contents: read`） | Task 10 で `.github/workflows/release.yml` を新設（`contents: write`） |
| D7 | 「`packages/mcp/src/desktop/`（新規）」（§3.3） | 追加するのは 3 ファイルのみで、`packages/mcp/src/` は現在フラット（`index.ts` + `index.spec.ts`） | サブディレクトリを作らず `setup-status.ts` / `setup-prompt.ts` / `confirm-phrase.ts` をフラットに置く。**spec §3.3 の意図（`index.ts` を太らせない）は満たしている**。起動配線は `packages/desktop-ext` 側に置いたので `mcp/src/desktop/` 自体が不要になった |
| D8 | バージョン同期対象に「研修受講者向け手順書（非公開 `AIftp/docs/` 側）」を含む（§6.3） | 非公開ワークスペース側にあり、リポジトリの CI からは参照できない | **自動検証の対象外**。Task 14 Step 8（memory.md 更新）と同じタイミングで**手動更新する**ことを Task 14 に明記した。CI ガード（Task 9）が守るのは公開リポジトリ内の 4 対象のみ |

---

## Global Constraints

以下はすべてのタスクの要件に暗黙に含まれる。値は spec とリポジトリ実測から逐語コピー。

- **Node**: `>=22.0.0`（全 package.json の `engines`）。TS ターゲット `ES2022`、`module: ESNext`、`moduleResolution: Bundler`、`verbatimModuleSyntax: true`、`noUncheckedIndexedAccess: true`
- **ESM only**: 全パッケージ `"type": "module"`。相対 import は必ず `.js` 拡張子付き
- **テスト配置**: `packages/*/src/**/*.spec.ts` のみ vitest が拾う（`vitest.config.ts:7`）。`scripts/` 配下や `tests/` 配下は**拾われない**
- **テスト環境**: vitest が `NODE_ENV=test` と `AIFTP_TEST_NO_REAL_KEYCHAIN=1` を注入する。**実 Keychain を spawn するとテストは fail-closed で落ちる**。必ず fake を注入する
- **HOME 隔離**: `HOME` と `USERPROFILE` を**両方**差し替える（`packages/mcp/src/index.spec.ts:36-66` のパターンを踏襲）。片方だけでは Windows で漏れる
- **アサーション**: 「形」ではなく**実値**を比較する（v0.12.4 の 1.4% 誤検知の教訓）
- **カバレッジ閾値**: lines/statements/functions 80%、branches 75%（`vitest.config.ts:20-25`。グローバル適用）
- **Lint**: biome 1.9.4、シングルクォート、セミコロンあり、`trailingCommas: all`、`lineWidth: 100`、`indentWidth: 2`、`lineEnding: lf`。`noExplicitAny: error`、`useImportType: error`、`useNodejsImportProtocol: error`
- **ファイル行数上限**: 800 行（規約）。`packages/mcp/src/index.ts` は既に 2519 行 — **本計画で追加するのは配線数行と `push_confirm` の引数 1 個のみ**。他は新規モジュールに置く
- **バージョン**: `0.13.0`。**5 つの package.json（ルート + core + mcp + cli + desktop-ext）を完全に同一の値で揃える**
- **publish**: 🔴 **`pnpm publish` を使う。`npm publish` は使わない**（v0.12.2 で `workspace:*` が未変換のまま公開され欠番化した事故）
- **セキュリティ非交渉ルール**（プロジェクト CLAUDE.md）:
  - パスワードを CLI 引数で受け取らない（Keychain 経由のみ）
  - ログにパスワードを残さない（マスキング必須）
  - 認証情報含有ファイル（`wp-config.php` / `.env*` / `db.php`）は hard exclude
  - 暗号化はデフォルト ON。off にするオプションを作らない
  - 平文 FTP は警告必須
- **実機テスト**: 本番 `/public_html` 直下にテストファイルを置かない。**必ずテスト用サブディレクトリ**を使う
- **git**: push / tag / publish は**田中さんの明示承認が必要**。エージェントが勝手に push しない
- **エラー文言契約**: 機械可読な英語識別子（`site-mismatch:` 等）は維持し、日本語は `hint` フィールドに併記する
- **redaction 契約**: 秘匿対象は**認証情報のみ**（`remote_root` は出してよい）。本計画では合言葉を秘匿対象に追加する
- **命名**: 本計画のコードでは、認証情報そのものを指す識別子に `credential` を使い、`password` は「Claude Desktop の設定欄の名前」を指すときだけ使う。テストのダミー値は `fixtureValue` に統一する（実値ではないことを名前で示す）

---

## File Structure

### 新規作成

| ファイル | 責務 |
|---|---|
| `packages/core/src/init/keychain-name.ts` | `sanitizeKeychainSiteName` / `buildKeychainService`（cli から移設） |
| `packages/core/src/init/keychain-name.spec.ts` | 同テスト |
| `packages/core/src/init/gitignore.ts` | `ensureGitignoreEntry(cwd, options)`（cli から移設 + git 判定オプション） |
| `packages/core/src/init/gitignore.spec.ts` | 同テスト |
| `packages/core/src/bootstrap/types.ts` | `BootstrapInput` / `BootstrapDeps` / `BootstrapResult` / `BootstrapValidationError` |
| `packages/core/src/bootstrap/validate.ts` | 入力検証（protocol・site_name・local_root）と `hint` 生成 |
| `packages/core/src/bootstrap/index.ts` | `runBootstrap(input, deps)` 冪等本体 |
| `packages/core/src/bootstrap/validate.spec.ts` | 検証テスト |
| `packages/core/src/bootstrap/index.spec.ts` | 冪等性テスト |
| `packages/mcp/src/confirm-phrase.ts` | チャレンジ生成・ハッシュ・タイミング安全照合 |
| `packages/mcp/src/confirm-phrase.spec.ts` | 同テスト |
| `packages/mcp/src/setup-status.ts` | `aiftp_setup_status` の判定ロジック（純粋関数 + deps 注入） |
| `packages/mcp/src/setup-status.spec.ts` | 同テスト |
| `packages/mcp/src/setup-prompt.ts` | `aiftp_setup` prompt の本文生成 |
| `packages/mcp/src/setup-prompt.spec.ts` | 同テスト |
| `packages/desktop-ext/package.json` | private パッケージ定義 |
| `packages/desktop-ext/tsconfig.json` | 既存 3 パッケージと同一 |
| `packages/desktop-ext/src/manifest.ts` | `buildManifest(version)` — manifest.json を組み立てる純粋関数 |
| `packages/desktop-ext/src/manifest.spec.ts` | manifest 構造テスト |
| `packages/desktop-ext/src/server-entry.ts` | `.mcpb` の `server/index.js` になる起動エントリ |
| `packages/desktop-ext/src/server-entry.spec.ts` | 起動堅牢性テスト |
| `packages/desktop-ext/src/version-consistency.spec.ts` | 5 package.json + manifest + docs のバージョン突き合わせ |
| `packages/desktop-ext/scripts/build-mcpb.mjs` | 依存同梱 → manifest 出力 → `mcpb pack` |
| `packages/desktop-ext/icon.png` | 512×512 |
| `docs/desktop-extension.md` | 受講者・講師向けドキュメント |
| `.github/workflows/release.yml` | Release へ `.mcpb` を自動添付 |

### 変更

| ファイル | 変更内容 |
|---|---|
| `packages/cli/src/init-flow.ts:290-311` | 実装を削除し core から re-export |
| `packages/cli/src/index.ts:424-431` | `ensureGitignore` を削除し core の `ensureGitignoreEntry` を呼ぶ |
| `packages/core/src/index.ts` | `./init/*` と `./bootstrap/index.js` を re-export |
| `packages/mcp/src/index.ts` | 配線（`confirmPhrase` オプション / `aiftp_setup_status` 登録 / `aiftp_setup` prompt 登録）＋ `pushConfirmSchema` に `confirmation` 追加＋ `handlePushPrepare` / `handlePushConfirm` の合言葉分岐 |
| `.github/workflows/ci.yml` | `mcpb` ジョブを 1 本追加 |
| `.github/workflows/smoke.yml` | Release アセットの `.mcpb` 検証ステップを追加 |
| ルート / core / mcp / cli の `package.json` | `0.12.4` → `0.13.0` |
| `CHANGELOG.md` | `## [0.13.0] — YYYY-MM-DD` セクション + リンク定義 |

---

## Task 0: 前提の実機検証（ゲート）

**このタスクが不成立なら、以降の設計を見直す。単独で最初に実施する。コードは書かない。**

**Files:**
- Create: `docs/superpowers/plans/2026-07-30-v0.13-task0-findings.md`

**Interfaces:**
- Produces: Task 7（manifest の `runtimes.node`）と Task 8（docs の未署名警告の説明文）が、ここで実測した値に依存する。

- [ ] **Step 1: Claude Desktop 同梱 Node のバージョンを実測する**

最小の検証用 MCP サーバを作って Claude Desktop に読ませ、`process.version` を返させる。スクラッチ領域で作業する（リポジトリを汚さない）。

```bash
mkdir -p /tmp/node-probe/server && cd /tmp/node-probe
cat > server/index.js <<'EOF'
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
const server = new McpServer({ name: 'node-probe', version: '0.0.1' });
server.registerTool('probe_node', { description: 'Report the runtime Node version' }, async () => ({
  content: [{ type: 'text', text: JSON.stringify({ version: process.version, execPath: process.execPath, platform: process.platform, arch: process.arch }) }],
}));
await server.connect(new StdioServerTransport());
EOF
```

`package.json`（`{"name":"node-probe","version":"0.0.1","type":"module","dependencies":{"@modelcontextprotocol/sdk":"^1.29.0"}}`）を置いて `npm install`、`manifest.json` を書いて `npx @anthropic-ai/mcpb pack` する。

- [ ] **Step 2: 実機に入れて `probe_node` を呼ぶ**

期待: `version` が `v22.x` 以上。**`v22` 未満なら Task 7 の `compatibility.runtimes.node` を実測値に合わせ、`packages/*/package.json` の `engines` との齟齬を Task 8 の docs に明記する。それでも動かないなら設計の見直しをユーザーへ上申する。**

- [ ] **Step 3: 未署名 `.mcpb` の警告表示を記録する**

同じ `.mcpb` をダブルクリックし、インストール時のダイアログを **macOS / Windows 両方でスクリーンショット**する。文言をそのまま書き起こす。

- [ ] **Step 4: 自己署名の効果を確認する**

`npx @anthropic-ai/mcpb sign --self-signed` した `.mcpb` を同様にインストールし、Step 3 と**警告文言が変わるか**を記録する。変わらないなら spec §6.2 の想定どおり（未署名で出荷）。

- [ ] **Step 5: 所見を記録してコミット**

`docs/superpowers/plans/2026-07-30-v0.13-task0-findings.md` に、Node バージョン・警告文言（両 OS）・自己署名の効果を**実測値のまま**書く。推測を混ぜない。

```bash
git add docs/superpowers/plans/2026-07-30-v0.13-task0-findings.md
git commit -m "docs: record v0.13 Task 0 real-machine findings (bundled Node, unsigned warning)"
```

---

## Task 1: core へ init プリミティブを移設

**Files:**
- Create: `packages/core/src/init/keychain-name.ts`, `packages/core/src/init/keychain-name.spec.ts`
- Create: `packages/core/src/init/gitignore.ts`, `packages/core/src/init/gitignore.spec.ts`
- Modify: `packages/core/src/index.ts`（re-export 追加）
- Modify: `packages/cli/src/init-flow.ts:290-311`（実装削除 → re-export）
- Modify: `packages/cli/src/index.ts:424-431`（`ensureGitignore` 削除 → core 呼び出し）

**Interfaces:**
- Produces:
  - `sanitizeKeychainSiteName(siteName: string | undefined): string`
  - `buildKeychainService(siteName: string | undefined, profileName: string): string`
  - `ensureGitignoreEntry(cwd: string, options?: { requireGitRepo?: boolean }): Promise<'updated' | 'already-present' | 'skipped-not-a-repo'>`
- Consumes: なし（Task 0 と並行に着手可）

- [ ] **Step 1: keychain 命名の失敗するテストを書く**

`packages/core/src/init/keychain-name.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { buildKeychainService, sanitizeKeychainSiteName } from './keychain-name.js';

describe('sanitizeKeychainSiteName', () => {
  it('returns an empty string for undefined', () => {
    expect(sanitizeKeychainSiteName(undefined)).toBe('');
  });

  it('replaces path separators and colons with a single hyphen', () => {
    expect(sanitizeKeychainSiteName('a:/b\\c')).toBe('a-b-c');
  });

  it('collapses runs of hyphens and trims leading/trailing hyphens', () => {
    expect(sanitizeKeychainSiteName('  --my site--  ')).toBe('my-site');
  });

  it('keeps dots, underscores and hyphens', () => {
    expect(sanitizeKeychainSiteName('my_site.v2-a')).toBe('my_site.v2-a');
  });
});

describe('buildKeychainService', () => {
  it('builds a site-scoped service name', () => {
    expect(buildKeychainService('gwco', 'production')).toBe('aiftp:gwco-production');
  });

  it('falls back to a profile-only name when the site name sanitizes to empty', () => {
    expect(buildKeychainService('///', 'production')).toBe('aiftp:production');
  });

  it('falls back to a profile-only name when the site name is undefined', () => {
    expect(buildKeychainService(undefined, 'staging')).toBe('aiftp:staging');
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run packages/core/src/init/keychain-name.spec.ts`
Expected: FAIL — `Failed to resolve import "./keychain-name.js"`

- [ ] **Step 3: 実装を core へ移す**

`packages/core/src/init/keychain-name.ts` を新規作成し、`packages/cli/src/init-flow.ts:290-311` の本体を**そのまま**移す（関数名のみ `buildKeychainServiceInitial` → `buildKeychainService`）:

```ts
export function sanitizeKeychainSiteName(siteName: string | undefined): string {
  if (siteName === undefined) return '';
  // biome-ignore lint/suspicious/noControlCharactersInRegex: intentional keychain identifier sanitization
  const controlCharacters = /[\u0000-\u001f\u007f]+/gu;
  return siteName
    .trim()
    .replace(controlCharacters, '-')
    .replace(/[:/\\]+/gu, '-')
    .replace(/[^A-Za-z0-9._-]+/gu, '-')
    .replace(/-+/gu, '-')
    .replace(/^-+|-+$/gu, '');
}

export function buildKeychainService(
  siteName: string | undefined,
  profileName: string,
): string {
  const sanitizedSiteName = sanitizeKeychainSiteName(siteName);
  return sanitizedSiteName.length > 0
    ? `aiftp:${sanitizedSiteName}-${profileName}`
    : `aiftp:${profileName}`;
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run packages/core/src/init/keychain-name.spec.ts`
Expected: PASS（7 tests）

- [ ] **Step 5: `.gitignore` 保証の失敗するテストを書く**

`packages/core/src/init/gitignore.spec.ts`:

```ts
import { randomUUID } from 'node:crypto';
import { mkdir, readFile, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { afterEach, beforeEach, describe, expect, it } from 'vitest';
import { ensureGitignoreEntry } from './gitignore.js';

describe('ensureGitignoreEntry', () => {
  let cwd: string;

  beforeEach(async () => {
    cwd = join(tmpdir(), `aiftp-gitignore-${randomUUID()}`);
    await mkdir(cwd, { recursive: true });
  });

  afterEach(async () => {
    await rm(cwd, { recursive: true, force: true });
  });

  it('creates .gitignore with .aiftp/ when the file is absent', async () => {
    const outcome = await ensureGitignoreEntry(cwd);
    expect(outcome).toBe('updated');
    expect(await readFile(join(cwd, '.gitignore'), 'utf8')).toBe('.aiftp/\n');
  });

  it('appends a leading newline when the existing file lacks a trailing newline', async () => {
    await writeFile(join(cwd, '.gitignore'), 'node_modules', 'utf8');
    await ensureGitignoreEntry(cwd);
    expect(await readFile(join(cwd, '.gitignore'), 'utf8')).toBe('node_modules\n.aiftp/\n');
  });

  it('is idempotent when the entry already exists', async () => {
    await writeFile(join(cwd, '.gitignore'), 'node_modules\n.aiftp/\n', 'utf8');
    const outcome = await ensureGitignoreEntry(cwd);
    expect(outcome).toBe('already-present');
    expect(await readFile(join(cwd, '.gitignore'), 'utf8')).toBe('node_modules\n.aiftp/\n');
  });

  it('skips a non-git folder when requireGitRepo is true', async () => {
    const outcome = await ensureGitignoreEntry(cwd, { requireGitRepo: true });
    expect(outcome).toBe('skipped-not-a-repo');
    await expect(readFile(join(cwd, '.gitignore'), 'utf8')).rejects.toThrow();
  });

  it('writes into a git folder when requireGitRepo is true', async () => {
    await mkdir(join(cwd, '.git'), { recursive: true });
    const outcome = await ensureGitignoreEntry(cwd, { requireGitRepo: true });
    expect(outcome).toBe('updated');
    expect(await readFile(join(cwd, '.gitignore'), 'utf8')).toBe('.aiftp/\n');
  });
});
```

- [ ] **Step 6: テストが失敗することを確認**

Run: `pnpm vitest run packages/core/src/init/gitignore.spec.ts`
Expected: FAIL — `Failed to resolve import "./gitignore.js"`

- [ ] **Step 7: 実装を書く**

`packages/core/src/init/gitignore.ts`:

```ts
import { appendFile, readFile, stat } from 'node:fs/promises';
import { join } from 'node:path';

const IGNORE_ENTRY = '.aiftp/';

export type EnsureGitignoreOutcome = 'updated' | 'already-present' | 'skipped-not-a-repo';

export interface EnsureGitignoreOptions {
  /**
   * When true, do nothing unless `<cwd>/.git` exists. The Desktop bootstrap
   * sets this because a trainee's site folder is usually not a repository and
   * creating a stray .gitignore there is noise, not safety.
   * The CLI `init` keeps the historical behaviour (false) so an operator who
   * runs `aiftp init` before `git init` still gets the guard.
   */
  readonly requireGitRepo?: boolean;
}

async function isGitRepo(cwd: string): Promise<boolean> {
  try {
    return (await stat(join(cwd, '.git'))) !== undefined;
  } catch {
    return false;
  }
}

export async function ensureGitignoreEntry(
  cwd: string,
  options: EnsureGitignoreOptions = {},
): Promise<EnsureGitignoreOutcome> {
  if (options.requireGitRepo === true && !(await isGitRepo(cwd))) {
    return 'skipped-not-a-repo';
  }
  const path = join(cwd, '.gitignore');
  const source = await readFile(path, 'utf8').catch(() => '');
  if (source.split(/\r?\n/u).includes(IGNORE_ENTRY)) {
    return 'already-present';
  }
  const prefix = source.length > 0 && !source.endsWith('\n') ? '\n' : '';
  await appendFile(path, `${prefix}${IGNORE_ENTRY}\n`, 'utf8');
  return 'updated';
}
```

- [ ] **Step 8: テストが通ることを確認**

Run: `pnpm vitest run packages/core/src/init/gitignore.spec.ts`
Expected: PASS（5 tests）

- [ ] **Step 9: core の barrel に追加する**

`packages/core/src/index.ts` の末尾（`export * from './sites/index.js';` の直後）に追記:

```ts
export {
  buildKeychainService,
  sanitizeKeychainSiteName,
} from './init/keychain-name.js';
export {
  ensureGitignoreEntry,
  type EnsureGitignoreOptions,
  type EnsureGitignoreOutcome,
} from './init/gitignore.js';
```

- [ ] **Step 10: CLI を core へ委譲させる（挙動を変えない）**

`packages/cli/src/init-flow.ts` の 290-311 行（2 関数の本体）を削除し、代わりに:

```ts
import { buildKeychainService, sanitizeKeychainSiteName } from '@aiftp-tools/core';

export { sanitizeKeychainSiteName };
export const buildKeychainServiceInitial = buildKeychainService;
```

`packages/cli/src/index.ts` の 424-431 行（`ensureGitignore` 関数定義）を削除し、1919 行の呼び出しを差し替える:

```ts
      await ensureGitignoreEntry(cwd);
```

`ensureGitignoreEntry` を `@aiftp-tools/core` の import に追加し、未使用になった `appendFile` の import が他で使われていないか確認する（使われていなければ削除。biome の `noUnusedImports: error` が検出する）。

- [ ] **Step 11: 全体が緑であることを確認**

```bash
pnpm build && pnpm typecheck && pnpm lint && pnpm test
```
Expected: すべて PASS。既存テスト数は移設前と同数（挙動を変えていないため）。

- [ ] **Step 12: コミット**

```bash
git add packages/core/src/init packages/core/src/index.ts packages/cli/src/init-flow.ts packages/cli/src/index.ts
git commit -m "refactor: move init primitives (keychain naming, gitignore guard) from cli to core"
```

---

## Task 2: 冪等ブートストラップ（core）

**Files:**
- Create: `packages/core/src/bootstrap/types.ts`
- Create: `packages/core/src/bootstrap/validate.ts`, `packages/core/src/bootstrap/validate.spec.ts`
- Create: `packages/core/src/bootstrap/index.ts`, `packages/core/src/bootstrap/index.spec.ts`
- Modify: `packages/core/src/index.ts`

**Interfaces:**
- Consumes: Task 1 の `buildKeychainService`, `ensureGitignoreEntry`
- Produces:
  - `runBootstrap(input: Partial<BootstrapInput>, deps?: BootstrapDeps): Promise<BootstrapResult>`
  - `validateBootstrapInput(raw: Partial<BootstrapInput>): BootstrapInput`（不正時 `BootstrapValidationError`（`.hint` を持つ））
  - 型 `BootstrapInput` / `BootstrapDeps` / `BootstrapResult` / `BootstrapRegistrySurface` / `CredentialOutcome`

- [ ] **Step 1: 型を定義する**

`packages/core/src/bootstrap/types.ts`:

```ts
import type { SiteEntry, SiteProtocol } from '../sites/types.js';

export interface BootstrapInput {
  readonly siteName: string;
  readonly localRoot: string;
  readonly host: string;
  readonly protocol: SiteProtocol;
  readonly username: string;
  readonly remoteRoot: string;
  readonly profileName: string;
  /** The value Claude Desktop passes in from its "パスワード" field, once. */
  readonly credential?: string;
}

export interface BootstrapRegistrySurface {
  list(): Promise<readonly SiteEntry[]>;
  add(entry: SiteEntry): Promise<readonly SiteEntry[]>;
}

export interface BootstrapDeps {
  readonly pathExists?: (path: string) => Promise<boolean>;
  readonly readTextFile?: (path: string) => Promise<string>;
  readonly writeTextFile?: (path: string, contents: string) => Promise<void>;
  readonly storeCredential?: (service: string, account: string, value: string) => Promise<void>;
  readonly credentialExists?: (service: string, account: string) => Promise<boolean>;
  readonly createRegistry?: () => BootstrapRegistrySurface;
  readonly ensureGitignore?: (cwd: string) => Promise<string>;
}

export type ConfigOutcome = 'created' | 'existing';
export type CredentialOutcome = 'stored' | 'already-stored' | 'missing';
export type RegistryOutcome = 'registered' | 'already-registered';

export interface BootstrapResult {
  readonly ok: boolean;
  readonly siteName: string;
  readonly profileName: string;
  readonly keychainService: string;
  readonly configPath: string;
  readonly config: ConfigOutcome;
  readonly credential: CredentialOutcome;
  readonly registry: RegistryOutcome;
  readonly gitignore: string;
  readonly missing: readonly string[];
  readonly hint?: string;
}

export class BootstrapValidationError extends Error {
  readonly hint: string;

  constructor(message: string, hint: string) {
    super(message);
    this.name = 'BootstrapValidationError';
    this.hint = hint;
  }
}
```

- [ ] **Step 2: 検証の失敗するテストを書く**

`packages/core/src/bootstrap/validate.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { BootstrapValidationError } from './types.js';
import { validateBootstrapInput } from './validate.js';

const valid = {
  siteName: 'gwco',
  localRoot: '/abs/site',
  host: 'ftp.example.test',
  protocol: 'ftps' as const,
  username: 'deployer',
  remoteRoot: '/public_html',
};

describe('validateBootstrapInput', () => {
  it('defaults the profile name to production', () => {
    expect(validateBootstrapInput(valid).profileName).toBe('production');
  });

  it('defaults the protocol to ftps when omitted', () => {
    const { protocol, ...withoutProtocol } = valid;
    expect(validateBootstrapInput(withoutProtocol).protocol).toBe('ftps');
  });

  it('rejects an unknown protocol with a Japanese hint', () => {
    let thrown: unknown;
    try {
      validateBootstrapInput({ ...valid, protocol: 'sftps' as never });
    } catch (error) {
      thrown = error;
    }
    expect(thrown).toBeInstanceOf(BootstrapValidationError);
    expect((thrown as BootstrapValidationError).message).toBe(
      'bootstrap-invalid: protocol must be one of ftps, sftp, ftp (got "sftps")',
    );
    expect((thrown as BootstrapValidationError).hint).toBe(
      'Claude Desktop の設定 → 拡張機能 → aiftp で「プロトコル」欄に ftps / sftp / ftp のいずれかを入力し、Claude Desktop を再起動してください。',
    );
  });

  it('rejects a site name containing a path separator', () => {
    expect(() => validateBootstrapInput({ ...valid, siteName: 'a/b' })).toThrow(
      'bootstrap-invalid: site_name must contain only letters, digits, dot, underscore, or hyphen',
    );
  });

  it('rejects a relative local_root', () => {
    expect(() => validateBootstrapInput({ ...valid, localRoot: './site' })).toThrow(
      'bootstrap-invalid: local_root must be an absolute path',
    );
  });

  it('rejects an empty host', () => {
    expect(() => validateBootstrapInput({ ...valid, host: '   ' })).toThrow(
      'bootstrap-invalid: host must not be empty',
    );
  });

  it('trims surrounding whitespace from text fields', () => {
    expect(validateBootstrapInput({ ...valid, host: '  ftp.example.test  ' }).host).toBe(
      'ftp.example.test',
    );
  });
});
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `pnpm vitest run packages/core/src/bootstrap/validate.spec.ts`
Expected: FAIL — `Failed to resolve import "./validate.js"`

- [ ] **Step 4: 検証を実装する**

`packages/core/src/bootstrap/validate.ts`:

```ts
import { isAbsolute } from 'node:path';
import { isValidProfileName } from '../config-edit.js';
import type { SiteProtocol } from '../sites/types.js';
import { type BootstrapInput, BootstrapValidationError } from './types.js';

const SITE_NAME_PATTERN = /^[A-Za-z0-9._-]+$/u;
const PROTOCOLS: readonly SiteProtocol[] = ['ftps', 'sftp', 'ftp'];
const DEFAULT_PROFILE = 'production';

function settingsHint(field: string): string {
  return `Claude Desktop の設定 → 拡張機能 → aiftp で「${field}」欄を修正し、Claude Desktop を再起動してください。`;
}

function requireText(value: unknown, name: string, field: string): string {
  const text = typeof value === 'string' ? value.trim() : '';
  if (text.length === 0) {
    throw new BootstrapValidationError(
      `bootstrap-invalid: ${name} must not be empty`,
      settingsHint(field),
    );
  }
  return text;
}

export function validateBootstrapInput(raw: Partial<BootstrapInput>): BootstrapInput {
  const siteName = requireText(raw.siteName, 'site_name', 'サイト名');
  if (!SITE_NAME_PATTERN.test(siteName) || siteName === '.' || siteName === '..') {
    throw new BootstrapValidationError(
      'bootstrap-invalid: site_name must contain only letters, digits, dot, underscore, or hyphen',
      settingsHint('サイト名'),
    );
  }

  const localRoot = requireText(raw.localRoot, 'local_root', 'サイトフォルダ');
  if (!isAbsolute(localRoot)) {
    throw new BootstrapValidationError(
      'bootstrap-invalid: local_root must be an absolute path',
      'Claude Desktop の設定 → 拡張機能 → aiftp で「サイトフォルダ」をフォルダ選択ボタンから選び直し、Claude Desktop を再起動してください。',
    );
  }

  const protocolRaw = raw.protocol === undefined ? 'ftps' : String(raw.protocol).trim();
  if (!PROTOCOLS.includes(protocolRaw as SiteProtocol)) {
    throw new BootstrapValidationError(
      `bootstrap-invalid: protocol must be one of ftps, sftp, ftp (got ${JSON.stringify(protocolRaw)})`,
      'Claude Desktop の設定 → 拡張機能 → aiftp で「プロトコル」欄に ftps / sftp / ftp のいずれかを入力し、Claude Desktop を再起動してください。',
    );
  }

  const profileName =
    raw.profileName === undefined ? DEFAULT_PROFILE : String(raw.profileName).trim();
  if (!isValidProfileName(profileName)) {
    throw new BootstrapValidationError(
      `bootstrap-invalid: profile name is not valid: ${JSON.stringify(profileName)}`,
      settingsHint('プロファイル名'),
    );
  }

  return {
    siteName,
    localRoot,
    host: requireText(raw.host, 'host', 'ホスト名'),
    protocol: protocolRaw as SiteProtocol,
    username: requireText(raw.username, 'username', 'ユーザー名'),
    remoteRoot: requireText(raw.remoteRoot, 'remote_root', 'サーバー側フォルダ'),
    profileName,
    ...(raw.credential === undefined ? {} : { credential: raw.credential }),
  };
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `pnpm vitest run packages/core/src/bootstrap/validate.spec.ts`
Expected: PASS（7 tests）

- [ ] **Step 6: ブートストラップ本体の失敗するテストを書く**

`packages/core/src/bootstrap/index.spec.ts`:

```ts
import { randomUUID } from 'node:crypto';
import { mkdir, readFile, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { afterEach, beforeEach, describe, expect, it } from 'vitest';
import type { SiteEntry } from '../sites/types.js';
import { runBootstrap } from './index.js';
import type { BootstrapDeps } from './types.js';

// Not a real credential — a fixture used to prove the value never reaches disk.
const fixtureValue = 'fixture-only-not-real';

const input = {
  siteName: 'gwco',
  host: 'ftp.example.test',
  protocol: 'ftps' as const,
  username: 'deployer',
  remoteRoot: '/public_html',
  profileName: 'production',
  credential: fixtureValue,
};

function fakeDeps(): BootstrapDeps & {
  readonly stored: Map<string, string>;
  readonly entries: SiteEntry[];
} {
  const stored = new Map<string, string>();
  const entries: SiteEntry[] = [];
  return {
    stored,
    entries,
    storeCredential: async (service, account, value) => {
      stored.set(`${service}\u0000${account}`, value);
    },
    credentialExists: async (service, account) => stored.has(`${service}\u0000${account}`),
    createRegistry: () => ({
      list: async () => entries,
      add: async (entry) => {
        entries.push(entry);
        return entries;
      },
    }),
  };
}

describe('runBootstrap', () => {
  let localRoot: string;

  beforeEach(async () => {
    localRoot = join(tmpdir(), `aiftp-bootstrap-${randomUUID()}`);
    await mkdir(localRoot, { recursive: true });
  });

  afterEach(async () => {
    await rm(localRoot, { recursive: true, force: true });
  });

  it('creates .aiftp.toml, stores the credential, and registers the site', async () => {
    const deps = fakeDeps();
    const result = await runBootstrap({ ...input, localRoot }, deps);

    expect(result.ok).toBe(true);
    expect(result.config).toBe('created');
    expect(result.credential).toBe('stored');
    expect(result.registry).toBe('registered');
    expect(result.keychainService).toBe('aiftp:gwco-production');
    expect(result.missing).toEqual([]);

    const toml = await readFile(join(localRoot, '.aiftp.toml'), 'utf8');
    expect(toml).toContain('schema = 2');
    expect(toml).toContain('[profile.production]');
    expect(toml).toContain('host = "ftp.example.test"');
    expect(toml).toContain('protocol = "ftps"');
    expect(toml).toContain('user = "deployer"');
    expect(toml).toContain('remote_root = "/public_html"');
    expect(toml).toContain('local_root = "."');
    expect(toml).toContain('keychain_service = "aiftp:gwco-production"');

    expect(deps.stored.get('aiftp:gwco-production\u0000deployer')).toBe(fixtureValue);
    expect(deps.entries).toEqual([
      { name: 'gwco', path: localRoot, default_profile: 'production' },
    ]);
  });

  it('never writes the credential into .aiftp.toml', async () => {
    await runBootstrap({ ...input, localRoot }, fakeDeps());
    const toml = await readFile(join(localRoot, '.aiftp.toml'), 'utf8');
    expect(toml).not.toContain(fixtureValue);
    expect(toml.toLowerCase()).not.toContain('pass');
  });

  it('is idempotent: a second run changes nothing', async () => {
    const deps = fakeDeps();
    await runBootstrap({ ...input, localRoot }, deps);
    const firstToml = await readFile(join(localRoot, '.aiftp.toml'), 'utf8');

    const second = await runBootstrap({ ...input, localRoot }, deps);

    expect(second.ok).toBe(true);
    expect(second.config).toBe('existing');
    expect(second.credential).toBe('already-stored');
    expect(second.registry).toBe('already-registered');
    expect(await readFile(join(localRoot, '.aiftp.toml'), 'utf8')).toBe(firstToml);
    expect(deps.entries).toHaveLength(1);
  });

  it('does not overwrite a hand-edited .aiftp.toml', async () => {
    const custom = 'schema = 2\n\n[profile.production]\nhost = "hand.edited.test"\n';
    await writeFile(join(localRoot, '.aiftp.toml'), custom, 'utf8');

    const result = await runBootstrap({ ...input, localRoot }, fakeDeps());

    expect(result.config).toBe('existing');
    expect(await readFile(join(localRoot, '.aiftp.toml'), 'utf8')).toBe(custom);
  });

  it('reports a missing credential with a Japanese hint instead of throwing', async () => {
    const { credential, ...withoutCredential } = input;
    const result = await runBootstrap({ ...withoutCredential, localRoot }, fakeDeps());

    expect(result.ok).toBe(false);
    expect(result.credential).toBe('missing');
    expect(result.missing).toEqual(['credential']);
    expect(result.hint).toBe(
      'Claude Desktop の設定 → 拡張機能 → aiftp で「パスワード」欄を入力し、Claude Desktop を再起動してください。',
    );
  });

  it('refuses to re-register the same site name pointing at a different folder', async () => {
    const deps = fakeDeps();
    deps.entries.push({ name: 'gwco', path: join(tmpdir(), 'somewhere-else') });

    await expect(runBootstrap({ ...input, localRoot }, deps)).rejects.toThrow(
      'bootstrap-conflict: site "gwco" is already registered for a different folder',
    );
  });

  it('fails when local_root does not exist', async () => {
    await rm(localRoot, { recursive: true, force: true });
    await expect(runBootstrap({ ...input, localRoot }, fakeDeps())).rejects.toThrow(
      'bootstrap-invalid: local_root does not exist',
    );
  });
});
```

- [ ] **Step 7: テストが失敗することを確認**

Run: `pnpm vitest run packages/core/src/bootstrap/index.spec.ts`
Expected: FAIL — `Failed to resolve import "./index.js"`

- [ ] **Step 8: ブートストラップ本体を実装する**

`packages/core/src/bootstrap/index.ts`:

```ts
import { readFile, stat, writeFile } from 'node:fs/promises';
import { join, resolve as resolvePath } from 'node:path';
import { appendProfileBlock } from '../config-edit.js';
import { ensureGitignoreEntry } from '../init/gitignore.js';
import { buildKeychainService } from '../init/keychain-name.js';
import { hasPassword, setPassword } from '../keychain.js';
import { SiteRegistry } from '../sites/registry.js';
import {
  type BootstrapDeps,
  type BootstrapInput,
  type BootstrapResult,
  BootstrapValidationError,
  type ConfigOutcome,
  type CredentialOutcome,
  type RegistryOutcome,
} from './types.js';
import { validateBootstrapInput } from './validate.js';

export * from './types.js';
export { validateBootstrapInput } from './validate.js';

const CREDENTIAL_HINT =
  'Claude Desktop の設定 → 拡張機能 → aiftp で「パスワード」欄を入力し、Claude Desktop を再起動してください。';

async function defaultPathExists(path: string): Promise<boolean> {
  try {
    return (await stat(path)) !== undefined;
  } catch {
    return false;
  }
}

export async function runBootstrap(
  rawInput: Partial<BootstrapInput>,
  deps: BootstrapDeps = {},
): Promise<BootstrapResult> {
  const input = validateBootstrapInput(rawInput);
  const pathExists = deps.pathExists ?? defaultPathExists;
  const readTextFile = deps.readTextFile ?? ((path: string) => readFile(path, 'utf8'));
  const writeTextFile =
    deps.writeTextFile ??
    ((path: string, contents: string) =>
      writeFile(path, contents, { encoding: 'utf8', mode: 0o600 }));
  const storeCredential = deps.storeCredential ?? setPassword;
  const credentialExists = deps.credentialExists ?? hasPassword;
  const createRegistry = deps.createRegistry ?? (() => new SiteRegistry());
  const ensureGitignore =
    deps.ensureGitignore ?? ((cwd: string) => ensureGitignoreEntry(cwd, { requireGitRepo: true }));

  if (!(await pathExists(input.localRoot))) {
    throw new BootstrapValidationError(
      `bootstrap-invalid: local_root does not exist: ${input.localRoot}`,
      'Claude Desktop の設定 → 拡張機能 → aiftp で「サイトフォルダ」を存在するフォルダに選び直し、Claude Desktop を再起動してください。',
    );
  }

  const keychainService = buildKeychainService(input.siteName, input.profileName);
  const configPath = join(input.localRoot, '.aiftp.toml');

  // 1. .aiftp.toml — never overwrite an existing file. A trainee (or the CLI)
  //    may have edited it; the extension must not clobber that.
  let config: ConfigOutcome;
  if (await pathExists(configPath)) {
    await readTextFile(configPath);
    config = 'existing';
  } else {
    const source = appendProfileBlock('schema = 2', input.profileName, {
      host: input.host,
      port: input.protocol === 'sftp' ? 22 : 21,
      protocol: input.protocol,
      user: input.username,
      remote_root: input.remoteRoot,
      local_root: '.',
      keychain_service: keychainService,
      server_kind: 'generic',
    });
    await writeTextFile(configPath, source);
    config = 'created';
  }

  // 2. credential — copy the env-provided value into aiftp's own keychain
  //    exactly once, then stop reading the env.
  let credential: CredentialOutcome;
  if (await credentialExists(keychainService, input.username)) {
    credential = 'already-stored';
  } else if (input.credential !== undefined && input.credential.length > 0) {
    await storeCredential(keychainService, input.username, input.credential);
    credential = 'stored';
  } else {
    credential = 'missing';
  }

  // 3. fleet registry
  const registrySurface = createRegistry();
  const entries = await registrySurface.list();
  const existing = entries.find((entry) => entry.name === input.siteName);
  let registry: RegistryOutcome;
  if (existing) {
    if (resolvePath(existing.path) !== resolvePath(input.localRoot)) {
      throw new BootstrapValidationError(
        `bootstrap-conflict: site "${input.siteName}" is already registered for a different folder (${existing.path})`,
        'Claude Desktop の設定 → 拡張機能 → aiftp で「サイト名」を別の名前に変えるか、「サイトフォルダ」を登録済みのフォルダに合わせてください。',
      );
    }
    registry = 'already-registered';
  } else {
    await registrySurface.add({
      name: input.siteName,
      path: input.localRoot,
      default_profile: input.profileName,
    });
    registry = 'registered';
  }

  const gitignore = await ensureGitignore(input.localRoot);
  const missing = credential === 'missing' ? ['credential'] : [];

  return {
    ok: missing.length === 0,
    siteName: input.siteName,
    profileName: input.profileName,
    keychainService,
    configPath,
    config,
    credential,
    registry,
    gitignore,
    missing,
    ...(credential === 'missing' ? { hint: CREDENTIAL_HINT } : {}),
  };
}
```

- [ ] **Step 9: テストが通ることを確認**

Run: `pnpm vitest run packages/core/src/bootstrap`
Expected: PASS（14 tests）

- [ ] **Step 10: core の barrel に追加する**

`packages/core/src/index.ts` の末尾に:

```ts
export {
  type BootstrapDeps,
  type BootstrapInput,
  type BootstrapRegistrySurface,
  type BootstrapResult,
  BootstrapValidationError,
  type ConfigOutcome,
  type CredentialOutcome,
  type RegistryOutcome,
  runBootstrap,
  validateBootstrapInput,
} from './bootstrap/index.js';
```

- [ ] **Step 11: 全体が緑であることを確認してコミット**

```bash
pnpm build && pnpm typecheck && pnpm lint && pnpm test
```

```bash
git add packages/core/src/bootstrap packages/core/src/index.ts
git commit -m "feat(core): add idempotent Desktop bootstrap"
```

---

## Task 2 修正条項（2026-07-31・田中さん裁定）

> **この節が上の Step 1〜11 に優先する。** Task 2 のレビューで、上の設計には「受講者が設定を打ち間違えると二度と直せない」欠陥があることが判明した。受講者の唯一の操作面は Claude Desktop の設定フォームであり、各所の日本語 hint が「設定を直して再起動してください」と案内しているのに実際には何も変わらない、という自己矛盾になっていた。裁定は **①設定画面を正として `.aiftp.toml` を差分更新する ②認証情報は入力があれば常に上書きする**。あわせてレビューが指摘した実装レベルの不具合2件も本条項で直す。

### A1. `local_root` を正規化する（`validate.ts`）

台帳の `sitePathSchema` は `.` / `..` セグメントを拒否するが、`validateBootstrapInput` は `isAbsolute` しか見ていない。`/Users/x/./site` は bootstrap を通過して `registry.add` の生 zod エラーになり、`hint` を持たない例外が漏れる（「`BootstrapValidationError` だけが漏れる」契約の破れ）。正規化して塞ぐ。

```ts
import { isAbsolute, resolve as resolvePath } from 'node:path';
```

```ts
  const localRootRaw = requireText(raw.localRoot, 'local_root', 'サイトフォルダ');
  if (!isAbsolute(localRootRaw)) {
    throw new BootstrapValidationError(
      'bootstrap-invalid: local_root must be an absolute path',
      'Claude Desktop の設定 → 拡張機能 → aiftp で「サイトフォルダ」をフォルダ選択ボタンから選び直し、Claude Desktop を再起動してください。',
    );
  }
  const localRoot = resolvePath(localRootRaw);
```

以後 `localRoot` は正規化済みの値だけを使う。`index.ts` 側の `resolvePath(input.localRoot)` は不要になるので削除する（比較値と台帳へ書く値が同じ正規化になる）。

### A2. 衝突チェックを副作用の前に移す（`index.ts`）

現行は `.aiftp.toml` 書き込みと Keychain 保存の**後**に `bootstrap-conflict` を投げるため、「別の名前にしてください」と言われた時点で新フォルダに config が残り、他サイトが所有する Keychain サービス名に値が書き込まれている。台帳の読み取りは純粋な read なので、`local_root` 存在チェックの直後へ移す。処理順を次に固定する:

1. `validateBootstrapInput`
2. `local_root` 存在チェック
3. **台帳を読み、同名エントリの衝突チェック（ここで throw しうる）**
4. `.aiftp.toml` の生成 or 差分更新
5. 認証情報の保存
6. 台帳への登録（3 で未登録と判っている場合のみ）
7. `.gitignore`

### A3. `ConfigOutcome` に `'updated'` を足す（`types.ts`）

```ts
export type ConfigOutcome = 'created' | 'updated' | 'existing';
```

### A4. 既存 `.aiftp.toml` を差分更新する（`index.ts`）

**bootstrap が所有する 5 フィールドだけ**を設定画面の値で上書きし、ユーザーが足した `[safety]` などのセクションには一切触れない。core が既に公開している `setProfileField` / `findProfileBlockRange`（`config-edit.ts`）を使う。フィールドごとの現在値比較はしない — 全フィールドを書いたうえで**文字列が変わったかどうか**で判定する（`setProfileField` は同じ値なら同じテキストを返すため冪等）。

```ts
import { appendProfileBlock, findProfileBlockRange, setProfileField } from '../config-edit.js';
```

```ts
  let config: ConfigOutcome;
  if (await pathExists(configPath)) {
    const current = await readTextFile(configPath);
    let next = current;
    // Reconcile only the fields the Desktop settings form owns. A profile the
    // extension did not create is left completely alone.
    if (findProfileBlockRange(next, input.profileName)) {
      const owned: ReadonlyArray<readonly [string, string]> = [
        ['host', input.host],
        ['user', input.username],
        ['protocol', input.protocol],
        ['remote_root', input.remoteRoot],
        ['keychain_service', keychainService],
      ];
      for (const [field, value] of owned) {
        next = setProfileField(next, input.profileName, field, JSON.stringify(value));
      }
    }
    if (next === current) {
      config = 'existing';
    } else {
      await writeTextFile(configPath, next);
      config = 'updated';
    }
  } else {
    // ...unchanged: appendProfileBlock('schema = 2', ...) then writeTextFile
    config = 'created';
  }
```

> `findProfileBlockRange` が該当プロファイルを見つけられない場合（受講者が別名のプロファイルだけを持つ config を置いていた等）は**何も書かず `'existing'`**。手編集の保護はここで担保される。

### A5. 認証情報は入力があれば常に上書きする（`index.ts`）

```ts
  let credential: CredentialOutcome;
  if (input.credential !== undefined && input.credential.trim().length > 0) {
    // The Desktop settings form is authoritative: a corrected or rotated
    // password must be able to reach the keychain. Re-storing the same value
    // on every launch is harmless.
    await storeCredential(keychainService, input.username, input.credential);
    credential = 'stored';
  } else if (await credentialExists(keychainService, input.username)) {
    credential = 'already-stored';
  } else {
    credential = 'missing';
  }
```

保存する値は **trim しない**（パスワードの前後の空白は有意になりうる）。空白のみの入力は「未入力」として扱う。仕様書 §4.2 の「初回起動後はパスワード欄を空にしてよい」は、空欄なら `already-stored` になるのでそのまま成立する。

### A6. テストの追加・変更（`index.spec.ts`）

**変更が必要な既存テスト:** 「does not overwrite a hand-edited `.aiftp.toml`」は A4 により**もう成立しない**（`[profile.production]` の `host` は上書きされる）。下記 1〜3 に置き換える。

追加・変更するテスト（すべて実値で比較すること）:

1. **既存 config の bootstrap 所有フィールドを更新する** — `schema = 2` + `[profile.production]`（`host = "wrong.example.test"` ほか全必須フィールド）を置いて実行 → `config` が `'updated'`、読み直した TOML が `host = "ftp.example.test"` を含み `wrong.example.test` を含まない
2. **ユーザーが足したセクションを保存する** — 上の config の末尾に `[safety]` + `max_files_per_push = 42` を足して実行 → 更新後も `max_files_per_push = 42` がそのまま残る
3. **別名プロファイルしか無い config には触れない** — `[profile.staging]` だけの config を置いて実行 → `config` が `'existing'` かつファイル内容が**バイト単位で不変**
4. **2 回目の実行で config が `'existing'` になる** — 既存の冪等テストを、2 回目の `config` が `'existing'`（`'updated'` ではない）であることの確認に更新
5. **認証情報を上書きする** — 1 回目を `fixtureValue` で、2 回目を別の値で実行 → `credential` が `'stored'`、fake の map に**新しい値**が入っている
6. **入力が無ければ既存を維持する** — 保存済みの状態で `credential` 無しで実行 → `'already-stored'`、map の値は不変
7. **空白のみの入力は未入力扱い** — `credential: '   '` で実行 → `'missing'`、map は空
8. **衝突は副作用の前に throw する** — 別フォルダに同名サイトを登録済みの状態で実行 → throw したうえで、`.aiftp.toml` が**作られていない**こと・fake の map が**空のまま**であることを assert
9. **`local_root` が正規化される** — `join(localRoot, '.', 'x', '..')` のような未正規化パスで実行 → 台帳エントリの `path` が `localRoot` と厳密一致

### A7. Task 4 への波及

`packages/mcp/src/setup-status.ts` の `config_file` チェックは 3 値になる。Task 4 Step 3 の該当行を次に読み替える:

```ts
  checks.push({
    id: 'config_file',
    status: 'pass',
    message:
      boot.config === 'created'
        ? '.aiftp.toml created'
        : boot.config === 'updated'
          ? '.aiftp.toml updated from the extension settings'
          : '.aiftp.toml already present',
  });
```

Task 4 の `StartupShape.bootstrap.config` は `string` のままでよい。

---

## Task 3: `packages/desktop-ext` 骨格と server entry

**Files:**
- Create: `packages/desktop-ext/package.json`, `packages/desktop-ext/tsconfig.json`
- Create: `packages/desktop-ext/src/server-entry.ts`, `packages/desktop-ext/src/server-entry.spec.ts`

**Interfaces:**
- Consumes: Task 2 の `runBootstrap`, `BootstrapValidationError`
- Produces:
  - `readDesktopEnv(env: NodeJS.ProcessEnv): Partial<BootstrapInput>`
  - `startDesktopServer(env: NodeJS.ProcessEnv, deps?: DesktopServerDeps): Promise<DesktopStartupReport>`
  - `DesktopStartupReport = { bootstrap?: BootstrapResult; error?: { message: string; hint: string } }`
  - 環境変数 `AIFTP_DESKTOP_STARTUP` に `DesktopStartupReport` を JSON で載せる（Task 4 が読む）

- [ ] **Step 1: パッケージ定義を作る**

`packages/desktop-ext/package.json`（**`private: true` かつ `publishConfig` なし** — `workspace:*` が npm 公開で未変換になる v0.12.2 の事故を構造的に避ける）:

```json
{
  "name": "@aiftp-tools/desktop-ext",
  "version": "0.12.4",
  "private": true,
  "description": "aiftp Claude Desktop extension (.mcpb) — build-only package, never published to npm",
  "license": "MIT",
  "author": "Tanaka Yuichiro <u16.tanaka@gmail.com>",
  "type": "module",
  "main": "./dist/server-entry.js",
  "types": "./dist/server-entry.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build:mcpb": "node scripts/build-mcpb.mjs"
  },
  "dependencies": {
    "@aiftp-tools/cli": "workspace:*",
    "@aiftp-tools/core": "workspace:*"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^1.2.0"
  },
  "engines": { "node": ">=22.0.0" }
}
```

> `build:mcpb` を `build` と分けている理由: `pnpm -r build` は CI の 4 ジョブすべてで走る。`.mcpb` パックは重いので専用ジョブ 1 本だけで実行する。

`packages/desktop-ext/tsconfig.json`（既存 3 パッケージと**完全に同一**）:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "composite": false
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "src/**/*.spec.ts", "dist", "node_modules"]
}
```

依存をインストールする:

```bash
pnpm install
```

- [ ] **Step 2: server entry の失敗するテストを書く**

`packages/desktop-ext/src/server-entry.spec.ts`:

```ts
import { randomUUID } from 'node:crypto';
import { mkdir, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { afterEach, beforeEach, describe, expect, it } from 'vitest';
import { readDesktopEnv, startDesktopServer } from './server-entry.js';

// Not a real credential — a fixture used to prove the value never leaks.
const fixtureValue = 'fixture-only-not-real';

describe('readDesktopEnv', () => {
  it('maps the manifest env vars onto bootstrap input fields', () => {
    expect(
      readDesktopEnv({
        AIFTP_PROJECT_DIR: '/abs/site',
        AIFTP_BOOTSTRAP_SITE: 'gwco',
        AIFTP_BOOTSTRAP_HOST: 'ftp.example.test',
        AIFTP_BOOTSTRAP_PROTOCOL: 'ftps',
        AIFTP_BOOTSTRAP_USER: 'deployer',
        AIFTP_BOOTSTRAP_REMOTE_ROOT: '/public_html',
        AIFTP_BOOTSTRAP_CREDENTIAL: fixtureValue,
      }),
    ).toEqual({
      localRoot: '/abs/site',
      siteName: 'gwco',
      host: 'ftp.example.test',
      protocol: 'ftps',
      username: 'deployer',
      remoteRoot: '/public_html',
      credential: fixtureValue,
      profileName: 'production',
    });
  });

  it('omits fields whose env var is an empty string', () => {
    const result = readDesktopEnv({ AIFTP_PROJECT_DIR: '/abs/site', AIFTP_BOOTSTRAP_SITE: '' });
    expect(result.siteName).toBeUndefined();
    expect(result.localRoot).toBe('/abs/site');
  });
});

describe('startDesktopServer', () => {
  let localRoot: string;

  beforeEach(async () => {
    localRoot = join(tmpdir(), `aiftp-desktop-${randomUUID()}`);
    await mkdir(localRoot, { recursive: true });
  });

  afterEach(async () => {
    await rm(localRoot, { recursive: true, force: true });
    Reflect.deleteProperty(process.env, 'AIFTP_DESKTOP_STARTUP');
  });

  it('starts the MCP server even when every setting is missing', async () => {
    const started: Array<{ cwd: string }> = [];
    const report = await startDesktopServer(
      {},
      {
        startMcp: async (options) => {
          started.push(options);
        },
      },
    );

    expect(started).toHaveLength(1);
    expect(report.error?.message).toBe('bootstrap-invalid: site_name must not be empty');
    expect(report.error?.hint).toBe(
      'Claude Desktop の設定 → 拡張機能 → aiftp で「サイト名」欄を修正し、Claude Desktop を再起動してください。',
    );
  });

  it('starts the MCP server in the project directory when bootstrap succeeds', async () => {
    const started: Array<{ cwd: string }> = [];
    const report = await startDesktopServer(
      {
        AIFTP_PROJECT_DIR: localRoot,
        AIFTP_BOOTSTRAP_SITE: 'gwco',
        AIFTP_BOOTSTRAP_HOST: 'ftp.example.test',
        AIFTP_BOOTSTRAP_PROTOCOL: 'ftps',
        AIFTP_BOOTSTRAP_USER: 'deployer',
        AIFTP_BOOTSTRAP_REMOTE_ROOT: '/public_html',
        AIFTP_BOOTSTRAP_CREDENTIAL: fixtureValue,
      },
      {
        startMcp: async (options) => {
          started.push(options);
        },
        runBootstrap: async () => ({
          ok: true,
          siteName: 'gwco',
          profileName: 'production',
          keychainService: 'aiftp:gwco-production',
          configPath: join(localRoot, '.aiftp.toml'),
          config: 'created',
          credential: 'stored',
          registry: 'registered',
          gitignore: 'skipped-not-a-repo',
          missing: [],
        }),
      },
    );

    expect(started).toEqual([{ cwd: localRoot }]);
    expect(report.bootstrap?.ok).toBe(true);
    expect(report.error).toBeUndefined();
  });

  it('falls back to process.cwd() for the MCP cwd when the project dir is unset', async () => {
    const started: Array<{ cwd: string }> = [];
    await startDesktopServer(
      {},
      {
        startMcp: async (options) => {
          started.push(options);
        },
      },
    );
    expect(started[0]?.cwd).toBe(process.cwd());
  });

  it('never puts the credential into the startup report', async () => {
    const report = await startDesktopServer(
      { AIFTP_PROJECT_DIR: localRoot, AIFTP_BOOTSTRAP_CREDENTIAL: fixtureValue },
      { startMcp: async () => {} },
    );
    expect(JSON.stringify(report)).not.toContain(fixtureValue);
  });
});
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `pnpm vitest run packages/desktop-ext/src/server-entry.spec.ts`
Expected: FAIL — `Failed to resolve import "./server-entry.js"`

- [ ] **Step 4: server entry を実装する**

`packages/desktop-ext/src/server-entry.ts`:

```ts
import { createCli } from '@aiftp-tools/cli';
import {
  type BootstrapInput,
  type BootstrapResult,
  runBootstrap as realRunBootstrap,
} from '@aiftp-tools/core';

export interface DesktopServerDeps {
  readonly startMcp?: (options: { cwd: string }) => Promise<void>;
  readonly runBootstrap?: (input: Partial<BootstrapInput>) => Promise<BootstrapResult>;
}

export interface DesktopStartupReport {
  readonly bootstrap?: BootstrapResult;
  readonly error?: { readonly message: string; readonly hint: string };
}

const GENERIC_HINT =
  'Claude Desktop の設定 → 拡張機能 → aiftp の各項目を確認し、Claude Desktop を再起動してください。';

function text(value: string | undefined): string | undefined {
  if (value === undefined) return undefined;
  const trimmed = value.trim();
  return trimmed.length === 0 ? undefined : trimmed;
}

export function readDesktopEnv(env: NodeJS.ProcessEnv): Partial<BootstrapInput> {
  const fields: Array<[keyof BootstrapInput, string | undefined]> = [
    ['localRoot', text(env.AIFTP_PROJECT_DIR)],
    ['siteName', text(env.AIFTP_BOOTSTRAP_SITE)],
    ['host', text(env.AIFTP_BOOTSTRAP_HOST)],
    ['protocol', text(env.AIFTP_BOOTSTRAP_PROTOCOL)],
    ['username', text(env.AIFTP_BOOTSTRAP_USER)],
    ['remoteRoot', text(env.AIFTP_BOOTSTRAP_REMOTE_ROOT)],
    ['credential', text(env.AIFTP_BOOTSTRAP_CREDENTIAL)],
    ['profileName', text(env.AIFTP_BOOTSTRAP_PROFILE) ?? 'production'],
  ];
  const input: Record<string, unknown> = {};
  for (const [key, value] of fields) {
    if (value !== undefined) input[key] = value;
  }
  return input as Partial<BootstrapInput>;
}

/**
 * Design rule (spec §5.1): the server MUST always start. Claude Desktop shows
 * a bare "connection error" when the process exits, which is invisible to a
 * trainee and to the instructor. Every configuration problem is therefore
 * captured here and surfaced later through `aiftp_setup_status`.
 */
export async function startDesktopServer(
  env: NodeJS.ProcessEnv,
  deps: DesktopServerDeps = {},
): Promise<DesktopStartupReport> {
  const bootstrapFn =
    deps.runBootstrap ?? ((input: Partial<BootstrapInput>) => realRunBootstrap(input));
  const startMcp =
    deps.startMcp ??
    (async ({ cwd }: { cwd: string }) => {
      // Reuse the CLI's `mcp` command so the server gets the same runDoctor
      // wiring the terminal path has. Without it `aiftp_profile_test` refuses
      // every call (packages/mcp/src/index.ts handleProfileTest).
      // stdout/stderr are silenced because stdio is the MCP transport.
      await createCli({ cwd, stdout: () => {}, stderr: () => {} }).parseAsync([
        'node',
        'aiftp',
        'mcp',
      ]);
    });

  const input = readDesktopEnv(env);
  let report: DesktopStartupReport = {};
  try {
    report = { bootstrap: await bootstrapFn(input) };
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    const hint =
      typeof (error as { hint?: unknown }).hint === 'string'
        ? (error as { hint: string }).hint
        : GENERIC_HINT;
    report = { error: { message, hint } };
  }

  process.env.AIFTP_DESKTOP_STARTUP = JSON.stringify(report);
  await startMcp({ cwd: input.localRoot ?? process.cwd() });
  return report;
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `pnpm vitest run packages/desktop-ext/src/server-entry.spec.ts`
Expected: PASS（6 tests）

- [ ] **Step 6: `pnpm -r build` にパッケージが乗ることを確認**

```bash
pnpm build && pnpm typecheck && pnpm lint
```
Expected: `@aiftp-tools/desktop-ext` がビルド順の最後に現れ、`packages/desktop-ext/dist/server-entry.js` が生成される。

- [ ] **Step 7: コミット**

```bash
git add packages/desktop-ext pnpm-lock.yaml
git commit -m "feat(desktop-ext): add package skeleton and always-start server entry"
```

---

## Task 4: `aiftp_setup_status` ツール

**Files:**
- Create: `packages/mcp/src/setup-status.ts`, `packages/mcp/src/setup-status.spec.ts`
- Modify: `packages/mcp/src/index.ts`（ツール名 union / スキーマ / description / handlers / options）
- Modify: `packages/mcp/src/index.spec.ts`

**Interfaces:**
- Consumes: Task 3 の `process.env.AIFTP_DESKTOP_STARTUP`（JSON 文字列）
- Produces:
  - `buildSetupStatus(deps: SetupStatusDeps): Promise<SetupStatusReport>`
  - `SetupStatusReport = { ok: boolean; checks: readonly SetupCheck[] }`
  - `SetupCheck = { id: string; status: 'pass' | 'fail'; message: string; hint?: string }`
  - `AiftpMcpOptions.confirmPhrase?: string` と `AiftpMcpApp.confirmPhrase?: string`（Task 5 も使う）

- [ ] **Step 1: 失敗するテストを書く**

`packages/mcp/src/setup-status.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { buildSetupStatus } from './setup-status.js';

const phrase = 'sakura-2026';

const bootstrapOk = {
  ok: true,
  siteName: 'gwco',
  profileName: 'production',
  keychainService: 'aiftp:gwco-production',
  configPath: '/abs/site/.aiftp.toml',
  config: 'created',
  credential: 'stored',
  registry: 'registered',
  missing: [] as string[],
};

const happy = {
  startup: JSON.stringify({ bootstrap: bootstrapOk }),
  confirmPhrase: phrase,
  pathExists: async () => true,
};

describe('buildSetupStatus', () => {
  it('reports every check as pass in the happy path', async () => {
    const report = await buildSetupStatus(happy);
    expect(report.ok).toBe(true);
    expect(report.checks.map((check) => check.id)).toEqual([
      'bootstrap',
      'project_dir',
      'config_file',
      'credential',
      'registry',
      'confirm_phrase',
    ]);
    expect(report.checks.every((check) => check.status === 'pass')).toBe(true);
  });

  it('fails the credential check with a Japanese hint when it is not stored', async () => {
    const report = await buildSetupStatus({
      ...happy,
      startup: JSON.stringify({
        bootstrap: { ...bootstrapOk, ok: false, credential: 'missing', missing: ['credential'] },
      }),
    });
    const check = report.checks.find((entry) => entry.id === 'credential');
    expect(report.ok).toBe(false);
    expect(check?.status).toBe('fail');
    expect(check?.message).toBe('bootstrap-incomplete: credential not stored');
    expect(check?.hint).toBe(
      'Claude Desktop の設定 → 拡張機能 → aiftp で「パスワード」欄を入力し、Claude Desktop を再起動してください。',
    );
  });

  it('fails the confirm_phrase check when the phrase is unset', async () => {
    const report = await buildSetupStatus({ ...happy, confirmPhrase: undefined });
    const check = report.checks.find((entry) => entry.id === 'confirm_phrase');
    expect(report.ok).toBe(false);
    expect(check?.status).toBe('fail');
    expect(check?.message).toBe('bootstrap-incomplete: confirm phrase not set');
  });

  it('surfaces a startup error as the bootstrap check failure', async () => {
    const report = await buildSetupStatus({
      startup: JSON.stringify({
        error: {
          message: 'bootstrap-invalid: local_root must be an absolute path',
          hint: 'Claude Desktop の設定 → 拡張機能 → aiftp で「サイトフォルダ」をフォルダ選択ボタンから選び直し、Claude Desktop を再起動してください。',
        },
      }),
      confirmPhrase: phrase,
      pathExists: async () => false,
    });
    const bootstrap = report.checks.find((check) => check.id === 'bootstrap');
    expect(bootstrap?.status).toBe('fail');
    expect(bootstrap?.message).toBe('bootstrap-invalid: local_root must be an absolute path');
    expect(report.ok).toBe(false);
  });

  it('reports the extension as not configured when the startup report is absent', async () => {
    const report = await buildSetupStatus({
      startup: undefined,
      confirmPhrase: undefined,
      pathExists: async () => false,
    });
    expect(report.ok).toBe(false);
    expect(report.checks[0]?.message).toBe(
      'bootstrap-missing: the extension has not been configured yet',
    );
  });

  it('never leaks the confirm phrase into the report', async () => {
    const report = await buildSetupStatus(happy);
    expect(JSON.stringify(report)).not.toContain(phrase);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run packages/mcp/src/setup-status.spec.ts`
Expected: FAIL — `Failed to resolve import "./setup-status.js"`

- [ ] **Step 3: 実装する**

`packages/mcp/src/setup-status.ts`:

```ts
export interface SetupCheck {
  readonly id: string;
  readonly status: 'pass' | 'fail';
  readonly message: string;
  readonly hint?: string;
}

export interface SetupStatusReport {
  readonly ok: boolean;
  readonly checks: readonly SetupCheck[];
}

export interface SetupStatusDeps {
  readonly startup: string | undefined;
  readonly confirmPhrase: string | undefined;
  readonly pathExists: (path: string) => Promise<boolean>;
}

interface StartupShape {
  bootstrap?: {
    ok: boolean;
    siteName: string;
    configPath: string;
    config: string;
    credential: string;
    registry: string;
  };
  error?: { message: string; hint: string };
}

const SETTINGS = 'Claude Desktop の設定 → 拡張機能 → aiftp';
const RESTART = 'Claude Desktop を再起動してください。';

function parseStartup(raw: string | undefined): StartupShape | undefined {
  if (raw === undefined || raw.length === 0) return undefined;
  try {
    return JSON.parse(raw) as StartupShape;
  } catch {
    return undefined;
  }
}

export async function buildSetupStatus(deps: SetupStatusDeps): Promise<SetupStatusReport> {
  const startup = parseStartup(deps.startup);

  if (!startup) {
    return {
      ok: false,
      checks: [
        {
          id: 'bootstrap',
          status: 'fail',
          message: 'bootstrap-missing: the extension has not been configured yet',
          hint: `${SETTINGS} で各項目を入力し、${RESTART}`,
        },
      ],
    };
  }

  if (startup.error || !startup.bootstrap) {
    return {
      ok: false,
      checks: [
        {
          id: 'bootstrap',
          status: 'fail',
          message: startup.error?.message ?? 'bootstrap-missing: startup did not complete',
          hint: startup.error?.hint ?? `${SETTINGS} で各項目を入力し、${RESTART}`,
        },
      ],
    };
  }

  const boot = startup.bootstrap;
  const checks: SetupCheck[] = [
    { id: 'bootstrap', status: 'pass', message: `site "${boot.siteName}" bootstrapped` },
  ];

  checks.push(
    (await deps.pathExists(boot.configPath))
      ? { id: 'project_dir', status: 'pass', message: 'project directory is readable' }
      : {
          id: 'project_dir',
          status: 'fail',
          message: 'bootstrap-incomplete: project directory is not readable',
          hint: `${SETTINGS} で「サイトフォルダ」を選び直し、${RESTART}`,
        },
  );

  checks.push({
    id: 'config_file',
    status: 'pass',
    message: boot.config === 'created' ? '.aiftp.toml created' : '.aiftp.toml already present',
  });

  checks.push(
    boot.credential === 'missing'
      ? {
          id: 'credential',
          status: 'fail',
          message: 'bootstrap-incomplete: credential not stored',
          hint: `${SETTINGS} で「パスワード」欄を入力し、${RESTART}`,
        }
      : { id: 'credential', status: 'pass', message: 'credential stored in the OS keychain' },
  );

  checks.push({
    id: 'registry',
    status: 'pass',
    message:
      boot.registry === 'registered' ? 'site registered in the fleet' : 'site already registered',
  });

  const phraseSet = typeof deps.confirmPhrase === 'string' && deps.confirmPhrase.trim().length > 0;
  checks.push(
    phraseSet
      ? { id: 'confirm_phrase', status: 'pass', message: 'production confirm phrase is set' }
      : {
          id: 'confirm_phrase',
          status: 'fail',
          message: 'bootstrap-incomplete: confirm phrase not set',
          hint: `${SETTINGS} で「合言葉」欄に FTP のパスワードとは違う文字列を入力し、${RESTART}`,
        },
  );

  return { ok: checks.every((check) => check.status === 'pass'), checks };
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run packages/mcp/src/setup-status.spec.ts`
Expected: PASS（6 tests）

- [ ] **Step 5: MCP サーバに配線する**

`packages/mcp/src/index.ts` を編集する。

(a) import に追記（`stat` が未 import なら `node:fs/promises` の import にも追加）:
```ts
import { buildSetupStatus } from './setup-status.js';
```

(b) `AiftpToolName` の union（`index.ts:113-139`）の末尾に:
```ts
  | 'aiftp_setup_status'
```

(c) `toolSchemas`（`index.ts:376` 付近）に:
```ts
  aiftp_setup_status: noArgsSchema,
```
`toolDescriptions` に:
```ts
  aiftp_setup_status:
    'Report whether the Claude Desktop extension is fully configured: bootstrap, project directory, .aiftp.toml, keychain credential, fleet registration, production confirm phrase. Each failing check carries a Japanese `hint`. Credentials are never surfaced. Read-only.',
```

(d) `handleSitesList` の直前にハンドラを追加:
```ts
async function handleSetupStatus(app: AiftpMcpApp, rawArgs: unknown): Promise<CallToolResult> {
  noArgsSchema.parse(rawArgs ?? {});
  const report = await buildSetupStatus({
    startup: process.env.AIFTP_DESKTOP_STARTUP,
    confirmPhrase: app.confirmPhrase,
    pathExists: async (path: string) => {
      try {
        return (await stat(path)) !== undefined;
      } catch {
        return false;
      }
    },
  });
  return textResult(report, report.ok ? undefined : true);
}
```
`handlers`（`index.ts:2362-2389`）に:
```ts
  aiftp_setup_status: handleSetupStatus,
```

(e) `AiftpMcpOptions` に（Task 5 も使う共通の配線）:
```ts
export interface AiftpMcpOptions {
  cwd?: string;
  runtime?: AiftpMcpRuntime;
  /** Shared secret for the production push gate. Never echoed in tool output. */
  confirmPhrase?: string;
}
```
`AiftpMcpApp` に `readonly confirmPhrase?: string;` を足し、`createAiftpMcp`（`index.ts:2470-2476`）の app リテラルに:
```ts
    ...(() => {
      const phrase = options.confirmPhrase ?? process.env.AIFTP_CONFIRM_PHRASE;
      return phrase === undefined || phrase.length === 0 ? {} : { confirmPhrase: phrase };
    })(),
```

- [ ] **Step 6: 統合テストを追加する**

`packages/mcp/src/index.spec.ts` の末尾（最後の `});` の直前）に:

```ts
  it('aiftp_setup_status reports the missing credential with a Japanese hint', async () => {
    process.env.AIFTP_DESKTOP_STARTUP = JSON.stringify({
      bootstrap: {
        ok: false,
        siteName: 'gwco',
        profileName: 'production',
        keychainService: 'aiftp:gwco-production',
        configPath: join(cwd, '.aiftp.toml'),
        config: 'created',
        credential: 'missing',
        registry: 'registered',
        gitignore: 'skipped-not-a-repo',
        missing: ['credential'],
      },
    });
    await writeConfig();
    const app = createAiftpMcp({ cwd, confirmPhrase: 'sakura-2026' });
    const payload = parseText(await callAiftpTool(app, 'aiftp_setup_status', {}));

    expect(payload.ok).toBe(false);
    const check = (payload.checks as Array<Record<string, string>>).find(
      (entry) => entry.id === 'credential',
    );
    expect(check?.message).toBe('bootstrap-incomplete: credential not stored');
    expect(JSON.stringify(payload)).not.toContain('sakura-2026');
    Reflect.deleteProperty(process.env, 'AIFTP_DESKTOP_STARTUP');
  });
```

- [ ] **Step 7: 全体が緑であることを確認してコミット**

```bash
pnpm build && pnpm typecheck && pnpm lint && pnpm test
```

```bash
git add packages/mcp/src
git commit -m "feat(mcp): add aiftp_setup_status tool with Japanese hints"
```

---

## Task 5: 合言葉ゲート

**Files:**
- Create: `packages/mcp/src/confirm-phrase.ts`, `packages/mcp/src/confirm-phrase.spec.ts`
- Modify: `packages/mcp/src/index.ts`（`PreparedPushPlan` / `pushConfirmSchema` / `handlePushPrepare` / `handlePushConfirm`）
- Modify: `packages/mcp/src/index.spec.ts`

**Interfaces:**
- Consumes: Task 4 で追加した `app.confirmPhrase`
- Produces:
  - `generateChallenge(): string`（6 文字）
  - `hashConfirmation(challenge: string, phrase: string): string`
  - `verifyConfirmation(raw: string, expectedHash: string): boolean`

- [ ] **Step 1: 失敗するテストを書く**

`packages/mcp/src/confirm-phrase.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { generateChallenge, hashConfirmation, verifyConfirmation } from './confirm-phrase.js';

describe('generateChallenge', () => {
  it('returns six characters from an unambiguous alphabet', () => {
    for (let i = 0; i < 200; i += 1) {
      const challenge = generateChallenge();
      expect(challenge).toHaveLength(6);
      expect(challenge).toMatch(/^[A-HJ-NP-Z2-9]{6}$/u);
    }
  });

  it('does not repeat on consecutive calls', () => {
    const seen = new Set(Array.from({ length: 50 }, () => generateChallenge()));
    expect(seen.size).toBeGreaterThan(45);
  });
});

describe('verifyConfirmation', () => {
  const hash = hashConfirmation('AB3K9P', 'sakura-2026');

  it('accepts the exact challenge + phrase pair', () => {
    expect(verifyConfirmation('AB3K9P sakura-2026', hash)).toBe(true);
  });

  it('tolerates surrounding and repeated whitespace', () => {
    expect(verifyConfirmation('  AB3K9P   sakura-2026  ', hash)).toBe(true);
  });

  it('rejects a wrong challenge with the right phrase', () => {
    expect(verifyConfirmation('ZZ9Y2Q sakura-2026', hash)).toBe(false);
  });

  it('rejects the right challenge with a wrong phrase', () => {
    expect(verifyConfirmation('AB3K9P momiji-2026', hash)).toBe(false);
  });

  it('rejects both wrong', () => {
    expect(verifyConfirmation('ZZ9Y2Q momiji-2026', hash)).toBe(false);
  });

  it('rejects the challenge alone', () => {
    expect(verifyConfirmation('AB3K9P', hash)).toBe(false);
  });

  it('rejects the phrase alone', () => {
    expect(verifyConfirmation('sakura-2026', hash)).toBe(false);
  });

  it('rejects an empty string', () => {
    expect(verifyConfirmation('', hash)).toBe(false);
  });

  it('is case-sensitive on the phrase', () => {
    expect(verifyConfirmation('AB3K9P SAKURA-2026', hash)).toBe(false);
  });

  it('keeps internal whitespace inside a multi-word phrase', () => {
    const multi = hashConfirmation('AB3K9P', 'sakura no ki');
    expect(verifyConfirmation('AB3K9P sakura no ki', multi)).toBe(true);
    expect(verifyConfirmation('AB3K9P sakura  no ki', multi)).toBe(false);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run packages/mcp/src/confirm-phrase.spec.ts`
Expected: FAIL — `Failed to resolve import "./confirm-phrase.js"`

- [ ] **Step 3: 実装する**

`packages/mcp/src/confirm-phrase.ts`:

```ts
import { createHash, randomInt, timingSafeEqual } from 'node:crypto';

// I/O/0/1 are omitted so a trainee reading the challenge off the screen and
// typing it into chat cannot confuse them.
const ALPHABET = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
const CHALLENGE_LENGTH = 6;

export function generateChallenge(): string {
  let out = '';
  for (let i = 0; i < CHALLENGE_LENGTH; i += 1) {
    out += ALPHABET.charAt(randomInt(ALPHABET.length));
  }
  return out;
}

function normalize(raw: string): string {
  const trimmed = raw.trim();
  const separator = trimmed.search(/\s/u);
  if (separator < 0) return `${trimmed}\n`;
  return `${trimmed.slice(0, separator)}\n${trimmed.slice(separator + 1).trim()}`;
}

export function hashConfirmation(challenge: string, phrase: string): string {
  return createHash('sha256').update(`${challenge}\n${phrase}`, 'utf8').digest('hex');
}

export function verifyConfirmation(raw: string, expectedHash: string): boolean {
  const actual = createHash('sha256').update(normalize(raw), 'utf8').digest('hex');
  // Both operands are fixed-length sha256 digests, so timingSafeEqual never throws.
  return timingSafeEqual(Buffer.from(actual, 'hex'), Buffer.from(expectedHash, 'hex'));
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run packages/mcp/src/confirm-phrase.spec.ts`
Expected: PASS（12 tests）

- [ ] **Step 5: push フローに配線する**

`packages/mcp/src/index.ts`:

(a) import に追記:
```ts
import { generateChallenge, hashConfirmation, verifyConfirmation } from './confirm-phrase.js';
```

(b) `PreparedPushPlan`（`index.ts:726` 付近）に 1 フィールド追加:
```ts
  confirmationHash?: string;
```

(c) `pushConfirmSchema`（`index.ts:207-232`）の `.extend({...})` に追加:
```ts
    confirmation: z
      .string()
      .min(1)
      .optional()
      .describe(
        'Required when the prepare step returned confirmation_challenge. The human operator must type "<challenge> <phrase>" into chat; the phrase is a shared secret the model cannot know. Do not guess it and do not compose it yourself — wait for the operator to send it.',
      ),
```

(d) `handlePushPrepare`（`index.ts:920` 付近、`planStore.set` の直前）に:
```ts
  const confirmationChallenge =
    prodProfileWarning && app.confirmPhrase !== undefined ? generateChallenge() : undefined;
```
`planStore.set(planId, { ... })` のオブジェクト末尾に:
```ts
    ...(confirmationChallenge === undefined || app.confirmPhrase === undefined
      ? {}
      : { confirmationHash: hashConfirmation(confirmationChallenge, app.confirmPhrase) }),
```
`textResult({...})` の `prod_profile_warning` の直後に:
```ts
    ...(confirmationChallenge === undefined
      ? {}
      : {
          confirmation_challenge: confirmationChallenge,
          confirmation_instruction: `本番へ反映します。人間が次の1行をそのままチャットに入力してください: 「${confirmationChallenge} <合言葉>」。合言葉は Claude Desktop の設定に登録されたもので、AI は知りません。`,
        }),
```
`return textResult(...)` の直前にログ記録を入れる（`appendLogEntry` は既存、`index.ts:537`）:
```ts
  if (confirmationChallenge !== undefined) {
    await appendLogEntry(app.cwd, {
      at: new Date().toISOString(),
      event: 'confirm-phrase-challenge',
      profile: profileName,
      plan_id: planId,
    });
  }
```

(e) `handlePushConfirm`（`index.ts:994` の本番ゲートの直後）に:
```ts
  if (plan.confirmationHash !== undefined) {
    if (args.confirmation === undefined) {
      throw new Error(
        'confirmation-required: this production plan needs the human confirmation phrase. Ask the operator to type "<challenge> <phrase>" in chat, then pass it verbatim as `confirmation`.',
      );
    }
    if (!verifyConfirmation(args.confirmation, plan.confirmationHash)) {
      throw new Error(
        'confirmation-mismatch: the confirmation phrase did not match. Call aiftp_push_prepare again to obtain a fresh challenge.',
      );
    }
    await appendLogEntry(app.cwd, {
      at: new Date().toISOString(),
      event: 'confirm-phrase-accepted',
      profile: plan.profile,
      plan_id: plan.planId,
    });
  }
```

> **仕様上の意図的な非対称**: `app.confirmPhrase` が未設定なら `confirmationHash` は作られず、v0.12 の `acknowledge_production` のみのゲートがそのまま効く。CLI 経由の既存ユーザを壊さないため。この非対称は docs（Task 8 §4）に明記する。

- [ ] **Step 6: 統合テストを追加する**

`packages/mcp/src/index.spec.ts` の末尾に追記する。**先に既存ヘルパ `writeConfig`（`index.spec.ts:68`）のシグネチャを読み、`production` という名前のプロファイルを書ける形に合わせること**（引数を取らないなら、このテスト内で `.aiftp.toml` を直接書く）:

```ts
  it('gates a production push behind the confirmation phrase', async () => {
    await writeConfig({ profile: 'production' });
    const dryRun = createPushResult({ planned: ['index.html'] });
    const app = createAiftpMcp({
      cwd,
      confirmPhrase: 'sakura-2026',
      runtime: {
        runPush: async (opts) =>
          opts.dryRun ? dryRun : createPushResult({ uploaded: ['index.html'] }),
      },
    });

    const prepared = parseText(
      await callAiftpTool(app, 'aiftp_push_prepare', { profile: 'production' }),
    );
    expect(prepared.prod_profile_warning).toBe(true);
    expect(String(prepared.confirmation_challenge)).toMatch(/^[A-HJ-NP-Z2-9]{6}$/u);
    expect(JSON.stringify(prepared)).not.toContain('sakura-2026');

    const base = {
      profile: 'production',
      plan_id: prepared.plan_id,
      diff_hash: prepared.diff_hash,
      confirm_token: prepared.confirm_token,
      acknowledge_production: true,
    };

    const missing = parseText(await callAiftpTool(app, 'aiftp_push_confirm', base));
    expect(missing.error.message).toContain('confirmation-required:');

    const wrong = parseText(
      await callAiftpTool(app, 'aiftp_push_confirm', {
        ...base,
        confirmation: `${prepared.confirmation_challenge} wrong-phrase`,
      }),
    );
    expect(wrong.error.message).toContain('confirmation-mismatch:');

    const accepted = parseText(
      await callAiftpTool(app, 'aiftp_push_confirm', {
        ...base,
        confirmation: `${prepared.confirmation_challenge} sakura-2026`,
      }),
    );
    expect(accepted.ok).toBe(true);
  });

  it('keeps the v0.12 acknowledge_production behaviour when no phrase is configured', async () => {
    await writeConfig({ profile: 'production' });
    const dryRun = createPushResult({ planned: ['index.html'] });
    const app = createAiftpMcp({
      cwd,
      runtime: {
        runPush: async (opts) =>
          opts.dryRun ? dryRun : createPushResult({ uploaded: ['index.html'] }),
      },
    });

    const prepared = parseText(
      await callAiftpTool(app, 'aiftp_push_prepare', { profile: 'production' }),
    );
    expect(prepared.confirmation_challenge).toBeUndefined();

    const accepted = parseText(
      await callAiftpTool(app, 'aiftp_push_confirm', {
        profile: 'production',
        plan_id: prepared.plan_id,
        diff_hash: prepared.diff_hash,
        confirm_token: prepared.confirm_token,
        acknowledge_production: true,
      }),
    );
    expect(accepted.ok).toBe(true);
  });
```

- [ ] **Step 7: 全体が緑であることを確認してコミット**

```bash
pnpm build && pnpm typecheck && pnpm lint && pnpm test
```

```bash
git add packages/mcp/src
git commit -m "feat(mcp): gate production pushes behind a human confirmation phrase"
```

---

## Task 6: `aiftp_setup` prompt

**Files:**
- Create: `packages/mcp/src/setup-prompt.ts`, `packages/mcp/src/setup-prompt.spec.ts`
- Modify: `packages/mcp/src/index.ts`（`registerPrompt` を 1 箇所追加）
- Modify: `packages/mcp/src/index.spec.ts`

**Interfaces:**
- Consumes: Task 4 の `aiftp_setup_status`、Task 5 の合言葉ゲート（文面が参照する）
- Produces: `buildSetupPromptText(): string`

- [ ] **Step 1: 失敗するテストを書く**

`packages/mcp/src/setup-prompt.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { buildSetupPromptText } from './setup-prompt.js';

describe('buildSetupPromptText', () => {
  const text = buildSetupPromptText();

  it('names each tool the four onboarding steps use', () => {
    for (const tool of [
      'aiftp_setup_status',
      'aiftp_profile_test',
      'aiftp_push_prepare',
      'aiftp_push_confirm',
      'aiftp_backup_list',
      'aiftp_rollback_prepare',
      'aiftp_rollback_confirm',
    ]) {
      expect(text).toContain(tool);
    }
  });

  it('instructs the model to wait for the human to type the phrase', () => {
    expect(text).toContain('合言葉は AI には見えません');
    expect(text).toContain('人間が入力するまで待ってください');
  });

  it('tells the model to use a test subdirectory before production', () => {
    expect(text).toContain('テスト用サブディレクトリ');
  });

  it('is written in Japanese for the trainee-facing steps', () => {
    expect(text).toMatch(/[ぁ-んァ-ン一-龥]/u);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run packages/mcp/src/setup-prompt.spec.ts`
Expected: FAIL — `Failed to resolve import "./setup-prompt.js"`

- [ ] **Step 3: 実装する**

`packages/mcp/src/setup-prompt.ts`:

```ts
/**
 * The onboarding prompt maps 1:1 onto the training curriculum
 * (training-product-v0.1.md §4, 午後1 / 午後2).
 */
export function buildSetupPromptText(): string {
  return [
    'aiftp の初回セットアップを、次の4ステップで順に進めてください。各ステップが終わるたびに、結果を日本語で1〜2文にまとめて報告してください。',
    '',
    '## ① 接続確認',
    '`aiftp_setup_status` を呼び、fail のチェックがあれば、その `hint` をそのまま日本語で伝えて、そこで止まってください（先へ進まないこと）。すべて pass なら `aiftp_profile_test` で実際の接続を確認します。',
    '',
    '## ② テスト領域へ push',
    'いきなり本番へ出しません。まず `remote_root` の下の**テスト用サブディレクトリ**を対象に、`aiftp_push_prepare` → 内容を日本語で要約 → 人間の同意を得てから `aiftp_push_confirm` を呼びます。',
    '',
    '## ③ 本番反映',
    '`aiftp_push_prepare` に `expected_site` を必ず指定します。本番プロファイルなら `confirmation_challenge` が返ります。**合言葉は AI には見えません。** チャレンジコードを提示し、「<チャレンジ> <合言葉>」の形で人間が入力するまで待ってください。合言葉を推測したり、人間の代わりに組み立てたりしてはいけません。受け取った行をそのまま `confirmation` に渡します。',
    '',
    '## ④ 戻す体験',
    '`aiftp_backup_list` で自動取得されたバックアップを見せ、`aiftp_rollback_prepare` → `aiftp_rollback_confirm` で1つ前の状態に戻せることを実演します。',
    '',
    '困ったときは `aiftp_setup_status` に戻ってください。設定の不足は必ず `hint` に日本語の対処法が入っています。',
  ].join('\n');
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run packages/mcp/src/setup-prompt.spec.ts`
Expected: PASS（4 tests）

- [ ] **Step 5: prompt を登録する**

`packages/mcp/src/index.ts` の import に:
```ts
import { buildSetupPromptText } from './setup-prompt.js';
```

`createAiftpMcp` のツール登録ループ（`index.ts:2478-2487`）の直後に:
```ts
  app.server.registerPrompt(
    'aiftp_setup',
    {
      title: 'aiftp セットアップ',
      description:
        'Guide the operator through the four onboarding steps: connection check, test-area push, production push behind the confirmation phrase, and rollback.',
    },
    () => ({
      messages: [{ role: 'user', content: { type: 'text', text: buildSetupPromptText() } }],
    }),
  );
```

- [ ] **Step 6: SDK の内部フィールド名を実測してから統合テストを書く**

まず実測する:

```bash
node -e "
import('@modelcontextprotocol/sdk/server/mcp.js').then(({ McpServer }) => {
  const s = new McpServer({ name: 'probe', version: '0.0.1' });
  s.registerPrompt('x', { description: 'd' }, () => ({ messages: [] }));
  console.log(Object.keys(s).filter((k) => k.toLowerCase().includes('prompt')));
});
"
```

出力されたフィールド名を使って `packages/mcp/src/index.spec.ts` の末尾に:

```ts
  it('registers the aiftp_setup prompt', () => {
    const app = createAiftpMcp({ cwd });
    const registered = (app.server as unknown as Record<string, Record<string, unknown>>)
      ._registeredPrompts;
    expect(Object.keys(registered)).toContain('aiftp_setup');
  });
```

> `_registeredPrompts` は実測名に置き換えること。内部 API に触るのはここだけで、破れても本番挙動には影響しない。

- [ ] **Step 7: 全体が緑であることを確認してコミット**

```bash
pnpm build && pnpm typecheck && pnpm lint && pnpm test
```

```bash
git add packages/mcp/src
git commit -m "feat(mcp): add the aiftp_setup onboarding prompt"
```

---

## Task 7: manifest 生成と `.mcpb` ビルド

**Files:**
- Create: `packages/desktop-ext/src/manifest.ts`, `packages/desktop-ext/src/manifest.spec.ts`
- Create: `packages/desktop-ext/scripts/build-mcpb.mjs`
- Create: `packages/desktop-ext/icon.png`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: Task 0 で実測した Node バージョン、Task 3 の `server-entry`
- Produces: `buildManifest(version: string): DesktopManifest`、成果物 `packages/desktop-ext/dist/aiftp-<version>.mcpb`

- [ ] **Step 1: 失敗するテストを書く**

`packages/desktop-ext/src/manifest.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { buildManifest } from './manifest.js';

describe('buildManifest', () => {
  const manifest = buildManifest('0.13.0');

  it('carries the version it was given', () => {
    expect(manifest.version).toBe('0.13.0');
  });

  it('declares both target platforms and the Node floor', () => {
    expect(manifest.compatibility.platforms).toEqual(['darwin', 'win32']);
    expect(manifest.compatibility.runtimes.node).toBe('>=22.0.0');
  });

  it('passes every user_config value to the server through env', () => {
    const env = manifest.server.mcp_config.env;
    expect(Object.keys(env).sort()).toEqual([
      'AIFTP_BOOTSTRAP_CREDENTIAL',
      'AIFTP_BOOTSTRAP_HOST',
      'AIFTP_BOOTSTRAP_PROTOCOL',
      'AIFTP_BOOTSTRAP_REMOTE_ROOT',
      'AIFTP_BOOTSTRAP_SITE',
      'AIFTP_BOOTSTRAP_USER',
      'AIFTP_CONFIRM_PHRASE',
      'AIFTP_PROJECT_DIR',
    ]);
    expect(env.AIFTP_PROJECT_DIR).toBe('${user_config.local_root}');
    expect(env.AIFTP_BOOTSTRAP_SITE).toBe('${user_config.site_name}');
    expect(env.AIFTP_BOOTSTRAP_HOST).toBe('${user_config.host}');
    expect(env.AIFTP_BOOTSTRAP_PROTOCOL).toBe('${user_config.protocol}');
    expect(env.AIFTP_BOOTSTRAP_USER).toBe('${user_config.username}');
    expect(env.AIFTP_BOOTSTRAP_REMOTE_ROOT).toBe('${user_config.remote_root}');
    expect(env.AIFTP_BOOTSTRAP_CREDENTIAL).toBe('${user_config.password}');
    expect(env.AIFTP_CONFIRM_PHRASE).toBe('${user_config.confirm_phrase}');
  });

  it('marks exactly the two secrets as sensitive', () => {
    const sensitive = Object.entries(manifest.user_config)
      .filter(([, field]) => field.sensitive === true)
      .map(([key]) => key)
      .sort();
    expect(sensitive).toEqual(['confirm_phrase', 'password']);
  });

  it('uses a directory picker for the site folder', () => {
    expect(manifest.user_config.local_root?.type).toBe('directory');
  });

  it('lists the allowed protocol values in the description because user_config has no enum type', () => {
    expect(manifest.user_config.protocol?.type).toBe('string');
    expect(manifest.user_config.protocol?.description).toContain('ftps');
    expect(manifest.user_config.protocol?.description).toContain('sftp');
    expect(manifest.user_config.protocol?.description).toContain('ftp');
  });

  it('reserves the free identity name', () => {
    expect(manifest.name).toBe('aiftp');
  });

  it('points at the packed entry point', () => {
    expect(manifest.server.entry_point).toBe('server/index.js');
    expect(manifest.server.mcp_config.args).toEqual(['${__dirname}/server/index.js']);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run packages/desktop-ext/src/manifest.spec.ts`
Expected: FAIL — `Failed to resolve import "./manifest.js"`

- [ ] **Step 3: 実装する**

`packages/desktop-ext/src/manifest.ts`:

```ts
export interface UserConfigField {
  readonly type: 'string' | 'number' | 'boolean' | 'directory' | 'file';
  readonly title: string;
  readonly description: string;
  readonly required: boolean;
  readonly sensitive?: boolean;
  readonly default?: string;
}

export interface DesktopManifest {
  readonly manifest_version: string;
  readonly name: string;
  readonly display_name: string;
  readonly version: string;
  readonly description: string;
  readonly author: { readonly name: string; readonly email: string };
  readonly license: string;
  readonly repository: { readonly type: string; readonly url: string };
  readonly icon: string;
  readonly server: {
    readonly type: 'node';
    readonly entry_point: string;
    readonly mcp_config: {
      readonly command: string;
      readonly args: readonly string[];
      readonly env: Readonly<Record<string, string>>;
    };
  };
  readonly user_config: Readonly<Record<string, UserConfigField>>;
  readonly compatibility: {
    readonly platforms: readonly string[];
    readonly runtimes: { readonly node: string };
  };
}

/** Claude Desktop substitutes `${user_config.<key>}` at launch time. */
function fromUserConfig(key: string): string {
  return `\${user_config.${key}}`;
}

export function buildManifest(version: string): DesktopManifest {
  return {
    manifest_version: '0.3',
    name: 'aiftp',
    display_name: 'aiftp — FTP/FTPS 安全デプロイ',
    version,
    description:
      'AI が書いたコードを FTP/FTPS/SFTP のレンタルサーバへ安全に反映します。自動バックアップ・2段確認・認証情報ファイルの構造的除外つき。',
    author: { name: 'Tanaka Yuichiro', email: 'u16.tanaka@gmail.com' },
    license: 'MIT',
    repository: { type: 'git', url: 'https://github.com/aiftp-tools/aiftp' },
    icon: 'icon.png',
    server: {
      type: 'node',
      entry_point: 'server/index.js',
      mcp_config: {
        command: 'node',
        args: ['${__dirname}/server/index.js'],
        env: {
          AIFTP_PROJECT_DIR: fromUserConfig('local_root'),
          AIFTP_BOOTSTRAP_SITE: fromUserConfig('site_name'),
          AIFTP_BOOTSTRAP_HOST: fromUserConfig('host'),
          AIFTP_BOOTSTRAP_PROTOCOL: fromUserConfig('protocol'),
          AIFTP_BOOTSTRAP_USER: fromUserConfig('username'),
          AIFTP_BOOTSTRAP_REMOTE_ROOT: fromUserConfig('remote_root'),
          AIFTP_BOOTSTRAP_CREDENTIAL: fromUserConfig('password'),
          AIFTP_CONFIRM_PHRASE: fromUserConfig('confirm_phrase'),
        },
      },
    },
    user_config: {
      site_name: {
        type: 'string',
        title: 'サイト名',
        description: '台帳での識別子。英数字・ドット・アンダースコア・ハイフンのみ（例: gwco）',
        required: true,
      },
      local_root: {
        type: 'directory',
        title: 'サイトフォルダ',
        description: 'あなたのサイトのファイルが入っているパソコン上のフォルダ',
        required: true,
      },
      host: {
        type: 'string',
        title: 'ホスト名',
        description: 'レンタルサーバの FTP ホスト名（例: ftp.example.jp）',
        required: true,
      },
      protocol: {
        type: 'string',
        title: 'プロトコル',
        description: 'ftps / sftp / ftp のいずれか。通常は ftps のままにしてください',
        required: false,
        default: 'ftps',
      },
      username: {
        type: 'string',
        title: 'ユーザー名',
        description: 'FTP のユーザー名',
        required: true,
      },
      remote_root: {
        type: 'string',
        title: 'サーバー側フォルダ',
        description: '公開ディレクトリ（例: /public_html）',
        required: true,
      },
      password: {
        type: 'string',
        title: 'パスワード',
        description:
          'FTP のパスワード。OS のキーチェーンに保存され、初回起動後はこの欄を空にしてかまいません',
        required: true,
        sensitive: true,
      },
      confirm_phrase: {
        type: 'string',
        title: '合言葉',
        description:
          '本番反映のときにあなた自身がチャットへ入力する合言葉。FTP のパスワードとは別の文字列にしてください',
        required: true,
        sensitive: true,
      },
    },
    compatibility: {
      platforms: ['darwin', 'win32'],
      runtimes: { node: '>=22.0.0' },
    },
  };
}
```

> `compatibility.runtimes.node` は **Task 0 Step 2 の実測値に合わせる**。実測が 22 未満だった場合はここを実測値に下げ、Task 8 の docs にその事実を書く。

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run packages/desktop-ext/src/manifest.spec.ts`
Expected: PASS（8 tests）

- [ ] **Step 5: アイコンを用意する**

512×512 の PNG を `packages/desktop-ext/icon.png` に置く。既存のブランド資産が無ければ、単色背景に `aiftp` の文字だけの最小限のものを作る（デザインは v0.13 のスコープ外）。

- [ ] **Step 6: ビルドスクリプトを書く**

`packages/desktop-ext/scripts/build-mcpb.mjs`:

```js
#!/usr/bin/env node
// Assemble the .mcpb bundle:
//   1. `pnpm deploy --prod` materialises a real node_modules tree.
//      We deliberately do NOT bundle with esbuild: ssh2 (used by the SFTP
//      client) has dynamic requires and an optional native dependency
//      (cpu-features) that bundlers break. `cpu-features` is optional, so
//      omitting it lets one .mcpb run on both macOS and Windows via the
//      pure-JS crypto fallback.
//   2. Write manifest.json from the compiled buildManifest().
//   3. `mcpb pack`.

import { execFileSync } from 'node:child_process';
import { cp, mkdir, readFile, rm, writeFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { buildManifest } from '../dist/manifest.js';

const here = dirname(fileURLToPath(import.meta.url));
const pkgRoot = join(here, '..');
const repoRoot = join(pkgRoot, '..', '..');
const stage = join(pkgRoot, 'dist', 'stage');

const pkg = JSON.parse(await readFile(join(pkgRoot, 'package.json'), 'utf8'));
const { version } = pkg;
if (!/^\d+\.\d+\.\d+(?:-[\w.]+)?$/.test(version ?? '')) {
  throw new Error(`build-mcpb: package.json version "${version}" is not a valid semver string.`);
}

await rm(stage, { recursive: true, force: true });
await mkdir(stage, { recursive: true });

// 1. dependencies
execFileSync(
  'pnpm',
  ['--filter', '@aiftp-tools/desktop-ext', 'deploy', '--prod', '--legacy', join(stage, 'server')],
  { cwd: repoRoot, stdio: 'inherit' },
);

// The entry point Claude Desktop launches.
await writeFile(
  join(stage, 'server', 'index.js'),
  [
    "import { startDesktopServer } from './dist/server-entry.js';",
    '',
    'await startDesktopServer(process.env);',
    '',
  ].join('\n'),
  'utf8',
);

// 2. manifest + icon
await writeFile(
  join(stage, 'manifest.json'),
  `${JSON.stringify(buildManifest(version), null, 2)}\n`,
  'utf8',
);
await cp(join(pkgRoot, 'icon.png'), join(stage, 'icon.png'));

// 3. pack
execFileSync(
  'pnpm',
  ['exec', 'mcpb', 'pack', stage, join(pkgRoot, 'dist', `aiftp-${version}.mcpb`)],
  { cwd: pkgRoot, stdio: 'inherit' },
);

console.log(`[build-mcpb] wrote packages/desktop-ext/dist/aiftp-${version}.mcpb`);
```

- [ ] **Step 7: ビルドを実行して成果物を検証する**

```bash
pnpm build && pnpm --filter @aiftp-tools/desktop-ext build:mcpb
```
Expected: `packages/desktop-ext/dist/aiftp-0.12.4.mcpb` が生成される（バージョン bump は Task 8）。

起動の堅牢性を手で確かめる:

```bash
cd packages/desktop-ext/dist/stage && node server/index.js < /dev/null; echo "exit=$?"
```
Expected: 設定が空でも**スタックトレースを出さずに終了する**（stdin が閉じてサーバが終わるのは正常）。エラーが出たら Task 3 の「起動は必ず成功させる」が破れているので直す。

同梱物を確かめる:

```bash
unzip -l packages/desktop-ext/dist/aiftp-0.12.4.mcpb | head -30
```
Expected: `manifest.json` / `icon.png` / `server/index.js` / `server/node_modules/...` が含まれる。

> **`pnpm deploy` が失敗した場合のフォールバック**（pnpm 10 は `node-linker` 設定によって `deploy` を拒否することがある）: `pnpm pack` で core / mcp / cli / desktop-ext の tarball を作り、`npm install --omit=dev` で `stage/server` へ展開する方式に切り替える。**どちらを採ったかを Task 8 の docs に 1 行残す。**

- [ ] **Step 8: 生成物を git 管理外にする**

`.gitignore` に追記:

```
packages/desktop-ext/dist/
```

- [ ] **Step 9: コミット**

```bash
git add packages/desktop-ext .gitignore
git commit -m "feat(desktop-ext): generate manifest.json and pack the .mcpb bundle"
```

---

## Task 8: ドキュメント・CHANGELOG・バージョン bump

**Files:**
- Create: `docs/desktop-extension.md`
- Modify: `CHANGELOG.md`
- Modify: `package.json`, `packages/core/package.json`, `packages/mcp/package.json`, `packages/cli/package.json`, `packages/desktop-ext/package.json`

**Interfaces:**
- Consumes: Task 0 の実測所見（Node バージョン・警告文言）、Task 5 の合言葉ゲート仕様、Task 7 の同梱方式
- Produces: `docs/desktop-extension.md` に `- **対応バージョン**: 0.13.0` の行（Task 9 がこの行を実値で検証する）

- [ ] **Step 1: 5 つの package.json を 0.13.0 にする**

```bash
cd /Users/ytanaka/Projects/Web/AIftp/aiftp
node -e "
const { readFileSync, writeFileSync } = require('node:fs');
const files = ['package.json','packages/core/package.json','packages/mcp/package.json','packages/cli/package.json','packages/desktop-ext/package.json'];
for (const f of files) {
  writeFileSync(f, readFileSync(f,'utf8').replace('\"version\": \"0.12.4\"', '\"version\": \"0.13.0\"'));
}
"
grep -n '"version"' package.json packages/core/package.json packages/mcp/package.json packages/cli/package.json packages/desktop-ext/package.json
```
Expected: 5 行すべて `"version": "0.13.0"`

- [ ] **Step 2: `docs/desktop-extension.md` を書く**

必須の見出しと内容（spec §4.3 / §5.3 / §6.2 を反映）:

```markdown
# Claude Desktop 拡張（.mcpb）

- **対応バージョン**: 0.13.0
- **対応 OS**: macOS / Windows
- **ライセンス**: MIT（無料）

## 1. 導入手順（ターミナル不要）
.mcpb をダウンロード → ダブルクリック → 設定 UI の各項目を入力 → Claude Desktop を再起動。

## 2. 「検証されていない発行元」の警告について
この拡張は**未署名**で配布しています。
（Task 0 Step 3 で記録した実際の警告文言を macOS / Windows それぞれ引用する。）

署名しない理由と、それでも安全と言える根拠:
- MIT ライセンス・ソース全公開（https://github.com/aiftp-tools/aiftp）
- 通信はあなたのサーバとの間だけ。テレメトリ送信なし
- 認証情報は OS のキーチェーンにのみ保存される

## 3. 本番反映の「合言葉」
チャレンジコード + 合言葉の仕組み。**FTP のパスワードとは別の文字列にしてください。**

### 既知の限界
一度チャットに合言葉を入力すると、その会話の中では AI から見える状態で残ります。
**同じ会話の 2 回目以降は、AI が自力で「<新しいチャレンジ> <合言葉>」を組み立てられます。**
完全な防止には MCP の elicitation が必要ですが、Claude Desktop では未実装です（Claude Code のみ）。
そのため次の緩和策を採っています:
- 合言葉を FTP のパスワードとは別にする
- 合言葉が提示された事実を毎回 `.aiftp/log.jsonl` に記録する
- Claude Desktop のツール承認ダイアログを二重ゲートとして残す

## 4. 合言葉を設定しない場合
「合言葉」欄を空にすると、v0.12 と同じ `acknowledge_production` のみのゲートになります
（CLI 利用者との互換のため）。研修では**必ず設定してください**。

## 5. トラブルシューティング
`aiftp_setup_status` の各 fail と対処。エラー識別子は英語、対処は `hint` の日本語。

## 6. CLI 版との関係
同じ設定・同じ台帳・同じキーチェーンを共有します。片方で init したフォルダをもう片方から使えます。
```

**Task 0 の所見ファイルから警告文言を実際に引用すること。推測で書かない。** Task 7 Step 7 のフォールバックを採った場合は同梱方式も 1 行残す。

- [ ] **Step 3: CHANGELOG にセクションを追加する**

`CHANGELOG.md` の `## [Unreleased]` ブロックの直後（`---` の下）に、既存フォーマット（見出しの区切りは **em dash `—`**）で:

```markdown
## [0.13.0] — YYYY-MM-DD

**Minor release** — Claude Desktop 用の `.mcpb` 拡張を追加。ターミナルと Node.js の導入なしで aiftp を使えるようにし、本番反映に人間の合言葉ゲートを設けた。

### Added

- **Claude Desktop 拡張（`.mcpb`）** — GitHub Release に `aiftp-0.13.0.mcpb` を添付。ダブルクリックで導入でき、Node.js の導入も MCP 設定 JSON の手編集も不要。設定 UI で選んだサイトフォルダを起動時に冪等ブートストラップし（`.aiftp.toml` 生成・キーチェーン保存・サイト台帳登録）、以後は CLI 版とまったく同じ状態を共有する。**未署名配布**（理由と安全性の根拠は `docs/desktop-extension.md`）。
- **`aiftp_setup_status` ツール** — 拡張の設定が揃っているかを 6 項目（bootstrap / プロジェクトフォルダ / `.aiftp.toml` / キーチェーン / サイト台帳 / 合言葉）で報告する読み取り専用ツール。失敗した項目には**日本語の `hint`** が付く。設定不足でサーバ起動を失敗させると Claude Desktop は「接続エラー」としか表示しないため、起動は常に成功させ不足はここで返す設計。
- **`aiftp_setup` prompt** — 接続確認 → テスト領域へ push → 本番反映 → ロールバックの 4 ステップを案内する MCP prompt。本リポジトリ初の prompt。
- **本番 push の合言葉ゲート** — 本番プロファイルへの `aiftp_push_prepare` がプランごとのチャレンジコードを返し、`aiftp_push_confirm` は `confirmation: "<チャレンジ> <合言葉>"` の完全一致を要求する（タイミング安全比較・ハッシュのみ保持）。合言葉はツール出力に一切出ないため、**人間がチャットに入力するまで confirm は通らない**。提示と受理は `.aiftp/log.jsonl` に記録される。**同一会話内での再利用という既知の限界**は `docs/desktop-extension.md` に明記した。合言葉が未設定の場合は v0.12 と同じ `acknowledge_production` のみのゲートになる（CLI 利用者との互換）。

### Changed

- **init プリミティブを core へ移設** — キーチェーンのサービス名生成（`buildKeychainService`）と `.gitignore` への `.aiftp/` 追加（`ensureGitignoreEntry`）を `@aiftp-tools/cli` から `@aiftp-tools/core` へ移した。CLI の挙動は変わらない。Desktop 拡張が同じ規約を再実装せずに済むようにするための整理。

---
```

ファイル末尾のリンク定義も更新する:

```markdown
[Unreleased]: https://github.com/aiftp-tools/aiftp/compare/v0.13.0...HEAD
[0.13.0]: https://github.com/aiftp-tools/aiftp/releases/tag/v0.13.0
```

- [ ] **Step 4: 全体が緑であることを確認**

```bash
pnpm build && pnpm typecheck && pnpm lint && pnpm test
node packages/cli/dist/bin.js --version
```
Expected: `--version` が `0.13.0` を返す（`generate-version.mjs` が prebuild で走る）

- [ ] **Step 5: コミット**

```bash
git add package.json packages/core/package.json packages/mcp/package.json packages/cli/package.json packages/desktop-ext/package.json docs/desktop-extension.md CHANGELOG.md
git commit -m "docs: add Desktop extension guide and bump to 0.13.0"
```

---

## Task 9: バージョン一貫性の CI ガード

**Task 8 に依存する**（検証対象の docs が存在していること）。

**Files:**
- Create: `packages/desktop-ext/src/version-consistency.spec.ts`

**Interfaces:**
- Consumes: Task 7 の `buildManifest`、Task 8 の 5 つの package.json と `docs/desktop-extension.md` と `CHANGELOG.md`

- [ ] **Step 1: テストを書く**

`packages/desktop-ext/src/version-consistency.spec.ts`:

```ts
import { readFile } from 'node:fs/promises';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { describe, expect, it } from 'vitest';
import { buildManifest } from './manifest.js';

const repoRoot = join(dirname(fileURLToPath(import.meta.url)), '..', '..', '..');

async function versionOf(relativePath: string): Promise<string> {
  const pkg = JSON.parse(await readFile(join(repoRoot, relativePath), 'utf8')) as {
    version?: unknown;
  };
  expect(typeof pkg.version).toBe('string');
  return pkg.version as string;
}

describe('version consistency', () => {
  it('keeps all five package.json versions identical', async () => {
    const root = await versionOf('package.json');
    expect(root).toMatch(/^\d+\.\d+\.\d+(?:-[\w.]+)?$/u);
    for (const pkg of ['core', 'mcp', 'cli', 'desktop-ext']) {
      expect(await versionOf(`packages/${pkg}/package.json`)).toBe(root);
    }
  });

  it('stamps the manifest with the desktop-ext package version', async () => {
    const version = await versionOf('packages/desktop-ext/package.json');
    expect(buildManifest(version).version).toBe(version);
  });

  it('documents the same version in docs/desktop-extension.md', async () => {
    const version = await versionOf('package.json');
    const docs = await readFile(join(repoRoot, 'docs', 'desktop-extension.md'), 'utf8');
    const match = /^- \*\*対応バージョン\*\*: (.+)$/mu.exec(docs);
    expect(match?.[1]).toBe(version);
  });

  it('has a CHANGELOG section and release links for the current version', async () => {
    const version = await versionOf('package.json');
    const changelog = await readFile(join(repoRoot, 'CHANGELOG.md'), 'utf8');
    expect(changelog).toContain(`## [${version}] — `);
    expect(changelog).toContain(
      `[${version}]: https://github.com/aiftp-tools/aiftp/releases/tag/v${version}`,
    );
    expect(changelog).toContain(
      `[Unreleased]: https://github.com/aiftp-tools/aiftp/compare/v${version}...HEAD`,
    );
  });
});
```

- [ ] **Step 2: テストを実行する**

Run: `pnpm vitest run packages/desktop-ext/src/version-consistency.spec.ts`
Expected: PASS（4 tests）。落ちる場合は Task 8 の記載が実値とずれているので docs / CHANGELOG 側を直す（**テストを緩めない**）。

- [ ] **Step 3: 故意に壊して検知を確認する**

`packages/desktop-ext/package.json` の version を一時的に `0.13.1` にして実行:

Run: `pnpm vitest run packages/desktop-ext/src/version-consistency.spec.ts`
Expected: FAIL（`expected '0.13.1' to be '0.13.0'`）。確認後 `0.13.0` に戻し、もう一度実行して PASS を確認する。

- [ ] **Step 4: コミット**

```bash
git add packages/desktop-ext/src/version-consistency.spec.ts
git commit -m "test: assert version consistency across packages, manifest, docs and CHANGELOG"
```

---

## Task 10: CI（`.mcpb` ビルド検証・Release 添付・smoke 拡張）

**Files:**
- Create: `.github/workflows/release.yml`
- Modify: `.github/workflows/ci.yml`, `.github/workflows/smoke.yml`

**Interfaces:**
- Consumes: Task 7 の `build:mcpb`

- [ ] **Step 1: CI に `mcpb` ジョブを追加する**

`.github/workflows/ci.yml` の `jobs:` の末尾（`test:` ジョブの後）に追記。**既存の 4 ジョブには手を入れない**（`.mcpb` パックを 4 回走らせないため、macOS × Node 22 の 1 本だけにする）:

```yaml
  mcpb:
    name: Build .mcpb bundle
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6
        with:
          version: 10.30.1
          run_install: false

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build workspace
        run: pnpm build

      - name: Pack the extension
        run: pnpm --filter @aiftp-tools/desktop-ext build:mcpb

      - name: Verify the bundle
        shell: bash
        run: |
          version="$(node -p "require('./package.json').version")"
          bundle="packages/desktop-ext/dist/aiftp-${version}.mcpb"
          test -f "$bundle" || { echo "FAIL: $bundle not found"; exit 1; }
          unzip -l "$bundle" | grep -q 'manifest.json' || { echo "FAIL: manifest.json missing"; exit 1; }
          unzip -l "$bundle" | grep -q 'server/index.js' || { echo "FAIL: server/index.js missing"; exit 1; }
          unzip -p "$bundle" manifest.json > /tmp/packed-manifest.json
          node -e "
            const m = require('/tmp/packed-manifest.json');
            const expected = require('./package.json').version;
            if (m.version !== expected) { console.error('FAIL: manifest ' + m.version + ' != ' + expected); process.exit(1); }
            if (m.server.entry_point !== 'server/index.js') { console.error('FAIL: entry_point ' + m.server.entry_point); process.exit(1); }
            console.log('PASS: manifest ' + m.version);
          "

      - name: Boot the packed server without configuration
        shell: bash
        run: |
          # spec §5.1: the server must start even with no configuration at all.
          cd packages/desktop-ext/dist/stage
          node server/index.js < /dev/null > boot-stdout.log 2> boot-stderr.log || true
          if grep -qi 'Error:' boot-stderr.log; then
            echo "FAIL: the server reported an error on an unconfigured start"
            cat boot-stderr.log
            exit 1
          fi
          echo "PASS: unconfigured start did not error"

      - uses: actions/upload-artifact@v4
        with:
          name: aiftp-mcpb
          path: packages/desktop-ext/dist/*.mcpb
```

- [ ] **Step 2: リリースワークフローを新設する**

`.github/workflows/release.yml`（**リポジトリ初の `contents: write` ワークフロー**）:

```yaml
name: release

# Attach the .mcpb bundle to a published GitHub Release. Publishing itself
# (tag, `pnpm publish` x3, Release creation) stays manual and requires the
# maintainer's explicit approval — this workflow only adds the asset.

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  attach-mcpb:
    name: Attach .mcpb to the release
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.release.tag_name || github.ref }}

      - uses: pnpm/action-setup@v6
        with:
          version: 10.30.1
          run_install: false

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm --filter @aiftp-tools/desktop-ext build:mcpb

      - name: Verify the bundle version matches the tag
        if: github.event_name == 'release'
        shell: bash
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          expected="${RELEASE_TAG#v}"
          actual="$(node -p "require('./package.json').version")"
          if [ "$expected" != "$actual" ]; then
            echo "FAIL: tag $RELEASE_TAG does not match package version $actual"
            exit 1
          fi
          echo "PASS: $actual"

      - name: Upload the asset
        if: github.event_name == 'release'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: gh release upload "$RELEASE_TAG" packages/desktop-ext/dist/*.mcpb --clobber
```

- [ ] **Step 3: smoke に `.mcpb` 検証を追加する**

`.github/workflows/smoke.yml` の `smoke` ジョブの末尾（`MCP smoke (stdio + tools/list)` ステップの後）に追記。**smoke ジョブには pnpm もソースツリーのビルドも無い**ので、Release アセットを落として中身だけ検証する:

```yaml
      - name: .mcpb asset is attached and well-formed
        if: github.event_name == 'release' && runner.os == 'macOS' && matrix.node == 22
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          version="${RELEASE_TAG#v}"
          gh release download "$RELEASE_TAG" --pattern '*.mcpb' --dir ./mcpb-check
          bundle="./mcpb-check/aiftp-${version}.mcpb"
          test -f "$bundle" || { echo "FAIL: $bundle not attached to the release"; exit 1; }
          unzip -p "$bundle" manifest.json > ./mcpb-check/manifest.json
          node -e "
            const m = require('./mcpb-check/manifest.json');
            const expected = process.env.RELEASE_TAG.replace(/^v/, '');
            if (m.version !== expected) { console.error('FAIL: manifest ' + m.version + ' != ' + expected); process.exit(1); }
            if (m.name !== 'aiftp') { console.error('FAIL: manifest name ' + m.name); process.exit(1); }
            console.log('PASS: .mcpb ' + m.version);
          "
```

- [ ] **Step 4: ワークフローファイルの最低限の健全性を確認する**

```bash
for f in .github/workflows/*.yml; do
  node -e "
    const s = require('node:fs').readFileSync('$f','utf8');
    if (!/^name:/m.test(s)) { console.error('missing name: $f'); process.exit(1); }
    if (/\t/.test(s)) { console.error('tab character in YAML: $f'); process.exit(1); }
    console.log('ok $f');
  "
done
```
Expected: `ci.yml` / `release.yml` / `smoke.yml` の 3 行が ok

- [ ] **Step 5: コミット**

```bash
git add .github/workflows
git commit -m "ci: build and verify the .mcpb bundle, attach it to releases"
```

---

## Task 11: 実機検証 — macOS

**Files:**
- Create: `docs/v0.13-dogfood-checklist.md`

**Interfaces:**
- Consumes: Task 7 の `.mcpb` 成果物、Task 8 の docs
- Produces: 完了条件 7 項目の実測結果。**不具合を見つけたら修正タスクを起こし、修正後に本タスクをやり直す**（v0.12 で本番バグが 3 件見つかった経路）

**前提（非交渉）**: 実機テストは **`/glocalworks.co.jp/public_html/aiftp-test` などテスト用サブディレクトリ**で行う。本番 `/public_html` 直下にテストファイルを置かない。

- [ ] **Step 1: チェックリスト文書を作る**

`docs/v0.13-dogfood-checklist.md` に spec §7.2 の 7 項目を、それぞれ「操作 / 期待 / 実測 / 判定」の 4 列で書き出す。v0.12 の `docs/v0.12-dogfood-checklist.md` を雛形にする。

- [ ] **Step 2: `.mcpb` をインストールする**

```bash
open packages/desktop-ext/dist/aiftp-0.13.0.mcpb
```
記録: 警告ダイアログの文言、設定 UI に 8 項目すべてが出るか、`local_root` がフォルダピッカーになっているか。

- [ ] **Step 3: 完了条件 ①〜③ を確認する**

- ① インストール成功
- ② `aiftp_setup` prompt からステップ① → `aiftp_setup_status` が全 pass → `aiftp_profile_test` が接続成功
- ③ **テストサブディレクトリ**へ push 成功

- [ ] **Step 4: 完了条件 ④ — 合言葉ゲートを両方向で確認する**

- `confirmation` 未指定 → `confirmation-required:` で拒否される
- 誤った合言葉 → `confirmation-mismatch:` で拒否される
- 正しい「`<チャレンジ> <合言葉>`」→ 通る
- `.aiftp/log.jsonl` に `confirm-phrase-challenge` と `confirm-phrase-accepted` が記録されている

- [ ] **Step 5: 完了条件 ⑤〜⑦ を確認する**

- ⑤ `aiftp_backup_list` → `aiftp_rollback_prepare` / `_confirm` で復元が一致
- ⑥ Claude Desktop の設定でパスワード欄を**空にして再起動**しても動作が継続する（Keychain から読めている）
- ⑦ `wp-config.php` と `.env` をフォルダに置いて `aiftp_push_prepare` → **どちらも `planned` に現れない**

- [ ] **Step 6: 結果を記録してコミット**

実測値をそのままチェックリストに書く。**pass にできなかった項目を pass と書かない。**

```bash
git add docs/v0.13-dogfood-checklist.md
git commit -m "docs: record v0.13 macOS dogfood results"
```

---

## Task 12: 実機検証 — Windows

**Files:**
- Modify: `docs/v0.13-dogfood-checklist.md`

**前提**: Windows の検証環境が必要。**環境を確保できない場合は Step 5 に従い、ユーザーへ上申して指示を待つ**（macOS 先行リリースへ方針変更するかは田中さんの判断。エージェントが勝手に決めない）。

- [ ] **Step 1: 検証環境を確認する**

Windows 実機 / VM / Parallels のいずれかに Claude Desktop をインストールできることを確認する。できない場合は Step 5 へ。

- [ ] **Step 2: 同一の `.mcpb` をインストールする**

macOS で作った**まったく同じファイル**を使う（1 つの `.mcpb` が両 OS で動くことが設計の主張）。警告文言を記録する。

- [ ] **Step 3: 完了条件 7 項目を Windows で通す**

Task 11 の Step 3〜5 と同じ手順。**特に注意して見る点**:
- 認証情報が Windows Credential Manager に入るか（`cmdkey /list | findstr aiftp`）
- 台帳が `%USERPROFILE%\.aiftp\sites.toml` に書かれるか
- パス区切りが混在しても `resolveDestination` の cwd 照合が通るか（v0.12 で Windows のパス期待値バグを踏んでいる）

- [ ] **Step 4: 結果を記録してコミット**

```bash
git add docs/v0.13-dogfood-checklist.md
git commit -m "docs: record v0.13 Windows dogfood results"
```

- [ ] **Step 5: 環境が確保できない場合**

チェックリストに「Windows 未検証」と明記し、**リリース可否をユーザーへ上申して指示を待つ**。「たぶん動く」で先へ進まない。

---

## Task 13: Codex セキュリティ クロスレビュー

**Files:**
- Create: `docs/review/v0.13-security-review.md`

**Interfaces:**
- Consumes: Task 1〜7 の全変更

- [ ] **Step 1: レビュー範囲を提示して Codex に依頼する**

対象を明示する:
- 認証情報の経路: `AIFTP_BOOTSTRAP_CREDENTIAL`（env）→ Keychain への書き写し → 以後 env を読まないこと。env 値がログ・ツール出力・エラーメッセージに漏れないこと
- 合言葉ゲート: `confirm-phrase.ts` のタイミング安全性、ハッシュのみ保持していること、`app.confirmPhrase` がツール出力に出ないこと
- `aiftp_setup_status` の redaction（秘匿対象は認証情報のみ、`remote_root` は許可）
- `.mcpb` 同梱物に開発依存や不要ファイルが混入していないこと
- 合言葉未設定時のフォールバックが安全側に倒れているか

- [ ] **Step 2: 指摘を分類して対応する**

CRITICAL / HIGH は**必ず修正**。MEDIUM は判断して記録。LOW は v0.13.1 backlog へ。

- [ ] **Step 3: 結果を記録してコミット**

```bash
git add docs/review/v0.13-security-review.md
git commit -m "docs: record v0.13 Codex security cross-review"
```

---

## Task 14: リリース

**すべての外向き操作は田中さんの明示承認が必要。エージェントは提案と準備までを行う。**

- [ ] **Step 1: 最終確認**

```bash
cd /Users/ytanaka/Projects/Web/AIftp/aiftp
pnpm install --frozen-lockfile && pnpm build && pnpm typecheck && pnpm lint && pnpm test
node packages/cli/dist/bin.js --version
pnpm --filter @aiftp-tools/desktop-ext build:mcpb
```
Expected: 全緑、`--version` が `0.13.0`、`.mcpb` が生成される

- [ ] **Step 2: PR ドラフトを作る**

`docs/release/v0.13.0-pr-draft.md` に、コミット一覧・テスト数・完了条件の実測結果を書く。コミット一覧は必ず再生成する:

```bash
git log --oneline main..HEAD
```

- [ ] **Step 3: push と PR 作成の承認を得る**

**ユーザーに確認してから**実行する:

```bash
git push -u origin claude/v0.13-desktop-extension
```

- [ ] **Step 4: CI が全ジョブ green であることを確認する**

```bash
gh pr checks --watch
```
Expected: `test` 4 本 + `mcpb` 1 本がすべて success。Windows Node 22 の ftp-client 統合テストは既知の flaky なので、落ちたら `gh run rerun --failed`。

- [ ] **Step 5: merge → tag → publish（すべて承認後）**

```bash
gh pr merge --squash
git checkout main && git pull
git tag v0.13.0 && git push origin v0.13.0
```

publish は 🔴 **`pnpm publish` を使う**（`npm publish` は `workspace:*` を変換せず v0.12.2 で欠番事故）:

```bash
npm whoami
pnpm --filter @aiftp-tools/core publish --access public
pnpm --filter @aiftp-tools/mcp publish --access public
pnpm --filter @aiftp-tools/cli publish --access public
```

`npm whoami` を先に確認する（トークン失効時、scoped パッケージは E404 を返して原因が分かりにくい）。`E403 cannot publish over the previously published versions` は**失敗ではなく既に成功済み**なので、即リトライ・即バージョン上げをせず `npm view <pkg> versions --json` で実測してから判断する。

`@aiftp-tools/desktop-ext` は `private: true` なので publish されない（意図どおり）。

- [ ] **Step 6: GitHub Release を作成する**

```bash
gh release create v0.13.0 --title "v0.13.0" --notes-file docs/release/v0.13.0-notes.md
```
release ワークフローが `.mcpb` を自動添付する。**添付されたことを実際に確認する**:

```bash
gh release view v0.13.0 --json assets --jq '.assets[].name'
```
Expected: `aiftp-0.13.0.mcpb` が含まれる

- [ ] **Step 7: smoke が green であることを確認する**

```bash
gh run list --workflow smoke --limit 1
```
Expected: 全ジョブ success

- [ ] **Step 8: 非公開側のバージョン記載を手で合わせる（D8）**

研修受講者向け手順書（非公開ワークスペース `/Users/ytanaka/Projects/Web/AIftp/docs/` 側）に記載しているバージョンを `0.13.0` に更新する。**ここはリポジトリ外なので Task 9 の CI ガードが届かない** — リリースのたびに手で合わせる。

- [ ] **Step 9: `memory.md` を更新する**

今回の作業 / 検証結果 / 現在状態 / 次回作業 / 注意点 のみを残す。

---

## 依存関係

```
Task 0 (ゲート) ──┬──> Task 7 (manifest の node 要件)
                  └──> Task 8 (docs の警告文言)

Task 1 ──> Task 2 ──> Task 3 ──> Task 4 ──┬──> Task 5
                                           └──> Task 6

Task 3 ──> Task 7 ──> Task 8 ──> Task 9
                 └──> Task 10

Task 9, 10 ──> Task 11 ──> Task 12 ──> Task 13 ──> Task 14
```

Task 0 と Task 1 は並行可能。Task 5 と Task 6 は Task 4 の後なら並行可能。

---

## Task 5 修正条項（2026-08-02・田中さん裁定）

本セクションは Task 5 の Step 1〜末尾の記述および、その後 Claude が実装者に与えた
「合言葉未設定なら常に fail-closed」という裁定を**上書きする**。両者が矛盾する場合は本セクションが優先する。

### 経緯

Claude が「`app.confirmPhrase` 未設定なら本番 push を拒否（fail-closed）」と裁定したが、
これは Claude Desktop の受講者ケースだけを見た判断であり、**ターミナル / Claude Code から
`packages/mcp` を使う既存ユーザー**を考慮していなかった。彼らにとって合言葉未設定は
設計上の正常状態である。commit `2a81e42` の実装では、既存 v0.12 ユーザーが
`safety.prod_profile_patterns` に一致するプロファイルへ push する際、
`AIFTP_CONFIRM_PHRASE` を設定しない限り `acknowledge_production: true` でも拒否される
＝ npm 公開済みパッケージの破壊的変更になっていた。

田中さん裁定: **Desktop 経由のときだけ fail-closed。ターミナルは v0.12 の挙動を維持する。**

### 条項

- **B1. Desktop 判定を明示的な信号にする。** `AIFTP_DESKTOP_STARTUP` の有無で代用してはならない。
  それは「起動レポートの受け渡し方法」であって「Desktop で動いているか」ではなく、将来レポートの
  受け渡しを変えた瞬間に安全ゲートが黙って緩む — 本ブランチが3回踏んだ
  「注入専用で本番フォールバック無し＝本番で黙って no-op」と同じ壊れ方になる。

- **B2. `packages/desktop-ext/src/server-entry.ts` に `process.env.AIFTP_DESKTOP = '1'` を追加する。**
  既存の `AIFTP_DESKTOP_STARTUP` 書き込みの直近に置く。Task 3 はこの1行のためだけに開ける。
  それ以外の desktop-ext の変更は禁止。

- **B3. `AiftpMcpOptions` / `AiftpMcpApp` に `desktopMode?: boolean` を追加**し、
  `confirmPhrase` と同じ経路で通す。**本番フォールバックは `process.env.AIFTP_DESKTOP === '1'`**。
  注入専用にしてはならない。フェイクを注入しないテストで本番既定を必ず覆う。

- **B4. ゲートの分岐は以下のとおり。**

  | desktopMode | confirmPhrase | 本番プロファイルへの push |
  |---|---|---|
  | true | 有り | 合言葉ゲート（v0.13 の新挙動） |
  | true | 無し | **拒否（fail-closed）**。日本語で「設定 → 拡張機能 → aiftp の合言葉欄を入力し再起動」と案内 |
  | false | 有り | 合言葉ゲート（ターミナルでも明示設定すれば有効） |
  | false | 無し | **v0.12 の挙動を維持**（`acknowledge_production: true` で通る）。合言葉には一切言及しない |

- **B5. 拒否メッセージの文言を文脈に合わせる。** commit `2a81e42` の `index.ts:1009` は
  Claude Desktop の設定画面を前提にした文言をターミナル利用者にも出す。B4 の表に従い、
  Desktop 文言は `desktopMode === true` のときだけ出す。

- **B6. 「v0.12 の挙動を維持する」テストを復活させる。** 実装者が置き換えた
  ブリーフ由来のテストは、`desktopMode: false` かつ `confirmPhrase` 未設定という条件で
  再度成立させる。既存ユーザーの回帰検知はこのテストが担う。

- **B7. 実装者がフレーズを渡すよう書き換えた既存テスト2件を見直す。** B4 の表の下で
  本来どの行に属するテストだったかを判断し、合言葉を足す必要が無くなったものは元に戻す。
