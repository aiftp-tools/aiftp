# v0.13 実機ドッグフード チェックリスト

> **対象**: 田中さん（実機操作）。**目的**: Claude Desktop 拡張（`.mcpb`）を受講者と同じ手順で導入し、合言葉ゲートが実機で機能することを確認する。これが v0.13.0 リリース前の最後の作業。
> **検証物**: `packages/desktop-ext/dist/aiftp-0.13.0.mcpb`（main = `3e9830e` からビルド。sha1 `ac9adab0bc37dc8d70a2cecdfc661b80609dc344` / 4.7MB / 2973ファイル）
> **ビルド済みの実測**: 絶対パス 0件・ネイティブ `.node` 0件・`workspace:*` 残留 0件・テスト 1090 pass。

---

## 🔒 安全ガードレール（必ず守る）

- [ ] **本番 `/public_html` 直下にテストファイルを置かない**。実 push は**テスト用サブディレクトリ** `/glocalworks.co.jp/public_html/aiftp-test/` に限定する
- [ ] **GWco の本番プロジェクトフォルダを使わない**。拡張の初期設定は `.aiftp.toml` を差分更新するため、**専用のテストフォルダを新規に作る**（例 `~/Projects/Web/aiftp-dogfood-v013/`）
- [ ] **合言葉をターミナルの Claude Code セッションに貼らない**。生成も入力も田中さん自身が行う（AI が知った合言葉はゲートとして機能しない）
- [ ] 合言葉を**スライド・画面共有・配布物に映さない**
- [ ] 実 push の前に**必ず宛先バナー**（サイト名・ホスト・リモートパス）を目視する

---

## Step 0 — 事前準備

- [ ] `main = 3e9830e` / 作業ツリークリーンを確認
- [ ] `.mcpb` の存在を確認: `ls -lh packages/desktop-ext/dist/aiftp-0.13.0.mcpb`
- [ ] テスト用フォルダを新規作成し、中に適当な HTML を2〜3個置く（`index.html` / `about.html`）
- [ ] **合言葉を生成する（田中さん自身がターミナルで実行。AI に実行させない）**:
  ```bash
  node packages/cli/dist/bin.js confirm-phrase generate
  ```
  出力をパスワードマネージャに控える。この後の設定 UI に貼る

---

## Step 1 — 拡張のインストール（受講者と同じ導線）

- [ ] `aiftp-0.13.0.mcpb` をダブルクリック
- [x] **確認ダイアログの文言を目視して記録する** → **macOS で実測済み（2026-08-08）**
  - 実測結果: **赤い警告が出る**。「コンピュータ上のすべてのデータへのアクセス権が付与されます／表示されている開発者情報は Anthropic によって検証されていません／提供元が信頼できることを必ず確認してください」
  - Task 0 の「未署名を理由とする追加警告は出ない」は**誤りだった**（アプリ内の文字列定義だけを読み、実画面を見ていなかった）。`docs/desktop-extension.md` 2章を実測に合わせて修正済み
  - 収穫: 同じ画面の「必要な要件」が **「すべての要件を満たしています」** ＝ Node.js 要件がインストール前に画面で確認できる
  - 🔴 **研修上の含意**: 受講者は赤い警告を見る。事前説明がないと手が止まる。研修手順書に必ず入れる
  - Windows でも同じ警告が出るかを Step 8 で確認する
- [ ] インストール完了を確認

## Step 2 — 設定 UI 入力 → 再起動

設定 → 拡張機能 → aiftp で以下8項目を入力する。

- [ ] サイト名（例 `dogfood-v013`）
- [ ] サイトフォルダ（Step 0 で作ったテストフォルダ）
- [ ] ホスト名 / プロトコル / ユーザー名
- [ ] サーバー側フォルダ → **`/glocalworks.co.jp/public_html/aiftp-test`**
- [ ] パスワード
- [ ] 合言葉（Step 0 で生成したもの）
- [ ] **Claude Desktop を再起動する**

## Step 3 — 起動確認（`aiftp_setup_status`）

- [ ] Claude Desktop で「aiftp の設定状態を確認して」と依頼
- [ ] **6項目すべて `pass`** を確認: `bootstrap` / `project_dir` / `config_file` / `credential` / `registry` / `confirm_phrase`
- [ ] `fail` があれば `hint` の日本語が実際に対処に足りるかを評価してメモ（研修での躓きポイント）
- [ ] キーチェーンに `aiftp:dogfood-v013-*` が作られたことを確認

## Step 4 — dry-run（宛先バナー）→ **実施済み（2026-08-08）**

- [x] 「テストサイトに何が上がるか確認して」と依頼
- [x] **宛先バナー**にサイト名・ホスト・`/aiftp-test` が正しく出るか目視
- [x] アップロード予定ファイル数が Step 0 で置いたファイル数と一致するか（新規2件・更新0・削除0）

## Step 5 — 実 push（合言葉ゲート本番）★最重要 → **全項目 pass（2026-08-08〜09 実測）**

- [x] 「反映して」と依頼 → **6文字のチャレンジコード**が提示されることを確認
- [x] **間違った合言葉**を入力 → **その反映プランが破棄される**ことを確認（実測: テンプレートの山括弧 `< >` を付けたまま入力→不一致→プラン破棄。同じチャレンジでの再試行は不可）
- [x] AI に `aiftp_push_prepare` からやり直させ、**新しいチャレンジコード**が出ることを確認
- [x] チャレンジコード + 正しい合言葉で承認 → **push 成功**（2件 added / 削除0。自動バックアップ `2026-08-08T15-35-47-947Z-auto-c9f9adbe-…` 作成済み）
  - 途中経過: 合言葉承認後に TLS 段階で2回失敗（host 誤記 `stars.ne.jp` → 証明書 `*.star.ne.jp` と不一致）。`sv73.star.ne.jp` に修正後、3回目で成功
- [x] ブラウザで `https://glocalworks.co.jp/aiftp-test/` を開いて反映を目視（HTTP 200・v1 ページ表示を curl でも確認）
- [x] `.aiftp/log.jsonl` に `confirm-phrase-challenge` と `confirm-phrase-accepted` が記録されていることを確認（challenge→accepted のペアが3組。最終ペア 15:34:53 / 15:35:47 UTC が成功 push に対応）

## Step 6 — 接続先すり替えの拒否（`destination-changed`）

- [ ] `aiftp_push_prepare` でチャレンジを出させた状態で、**承認前に** `.aiftp.toml` の `remote_root` を手で書き換える
- [ ] 正しい合言葉で承認 → **`destination-changed` で拒否される**ことを確認
- [ ] `.aiftp.toml` を元に戻す

## Step 7 — ロールバック（合言葉不要・`acknowledge_production` 必須）

- [ ] ファイルを1つ変更して2回目の push（合言葉は Step 5 で消費済み＝同一会話では AI が承認しうる。この挙動自体が既知の限界として docs に明記済み）
- [ ] 「ロールバックして」と依頼 → **`acknowledge_production` なしでは拒否**されることを確認
- [ ] `acknowledge_production: true` で実行 → 1つ前の状態に戻ることを確認
- [ ] **合言葉を要求されない**ことを確認（設計どおり）

## Step 8 — Windows 実機（2026-08-12 実施）

> **配布物は macOS と同一ファイル**を使う（Windows 用の別ビルドは無い）。
> `packages/desktop-ext/dist/aiftp-0.13.0.mcpb` / sha1 `ac9adab0bc37dc8d70a2cecdfc661b80609dc344` / 4.7MB
> ⚠️ 同じフォルダに **旧 `aiftp-0.12.4.mcpb` があるので取り違えに注意**（ファイル名の `0.13.0` を必ず確認）

### 8-0 事前準備

- [ ] `.mcpb` を Windows 機へ転送（USB / クラウドストレージのいずれでも可）
- [ ] 転送後にハッシュ照合（PowerShell）— 一致しなければ転送やり直し
  ```powershell
  certutil -hashfile "<転送先パス>\aiftp-0.13.0.mcpb" SHA1
  ```
  期待値: `ac9adab0bc37dc8d70a2cecdfc661b80609dc344`
- [ ] Windows 側に**テスト用フォルダを新規作成**し、HTML を2〜3個置く（例 `C:\Users\<user>\Projects\aiftp-dogfood-win\` に `index.html` / `about.html`）
- [ ] 合言葉を Bitwarden から取り出して手元に用意（**生成し直さない** — 合言葉は拡張機能に1本なので macOS 側と共通）

> 🔴 **Windows テスト後に合言葉の再ローテーションが必要**。実 push すると合言葉が Windows 側 Claude Desktop の会話履歴に平文で残るため。v0.13.0 リリース後に実施する（ターミナル不要・Bitwarden で完結）。

### 8-1 インストール — 🔴 **ここがリリースゲート**

- [ ] `aiftp-0.13.0.mcpb` をダブルクリック
- [ ] 🔴 **「必要な要件」の表示を確認する**（**Node バージョン判定の主ゲート**）
  - **「すべての要件を満たしています」= Claude Desktop 自身が同梱 Node を `>=22.0.0` と照合して合格させた**という意味。macOS では実測でこう表示された
  - **要件未達の表示が出たら、そこでリリースを止める**（`compatibility.runtimes.node` が満たされていない）
- [ ] 赤い警告（「コンピュータ上のすべてのデータへのアクセス権…開発者情報は Anthropic によって検証されていません」）が Windows でも出るか目視 → **研修手順書に反映するため文言をメモ**
- [ ] 詳細画面の**右上のインストールボタンを押す**（macOS で「見ただけで入った気になる」段差があった箇所）

### 8-2 設定 UI → 再起動

macOS 側の設定と**衝突させない**ため、サイト名とリモートパスを分ける。

- [ ] サイト名 → **`dogfood-v013-win`**（macOS の `dogfood-v013` と別にする）
- [ ] サイトフォルダ → 8-0 で作った Windows のテストフォルダ。**バックスラッシュのまま入力する**（パス区切り混在の検証を兼ねる）
- [ ] ホスト名 → **`sv73.star.ne.jp`**（割り当て名では TLS 証明書 `*.star.ne.jp` と一致せず push が落ちる）
- [ ] プロトコル / ユーザー名 / パスワード
- [ ] サーバー側フォルダ → **`/glocalworks.co.jp/public_html/aiftp-test-win`**（macOS 側の `aiftp-test` と分ける。後始末も別に行う）
- [ ] 合言葉（8-0 で用意したもの）
- [ ] **Claude Desktop を再起動**

### 8-3 起動確認（`aiftp_setup_status`）

- [ ] 「aiftp の設定状態を確認して」と依頼
- [ ] **6項目すべて `pass`**: `bootstrap` / `project_dir` / `config_file` / `credential` / `registry` / `confirm_phrase`
- [ ] ⚠️ ここが pass する時点で **Node は実際に要件を満たして動いている**（同梱 Node で MCP サーバが起動できている＝8-1 の判定の実地裏付け）
- [ ] `fail` があれば `hint` の日本語が対処に足りるか評価してメモ

### 8-4 同梱 Node の実測（8-1 / 8-3 の裏付け・バージョン数値の記録）

- [ ] MCP ログで起動経路を確認。ログは既定で `%APPDATA%\Claude\logs\` 配下（見つからなければ `dir /s /b "%APPDATA%\Claude" | findstr /i "mcp"` で探す）
  ```powershell
  Select-String -Path "$env:APPDATA\Claude\logs\*.log" -Pattern "Using built-in Node.js for MCP server"
  ```
  → 出力されたパスを控える（`server.type: "node"` が同梱 Node で起動している証拠）
- [ ] バージョン数値を取る。上のパスが `node.exe` 相当なら直接:
  ```powershell
  & "<上で得たパス>" --version
  ```
- [ ] 直接実行できない場合は、macOS で成功した「バイナリ内の文字列を読む」方式の Windows 版:
  ```powershell
  $p = "<Claude Desktop の Electron バイナリ>"
  [System.Text.Encoding]::ASCII.GetString([System.IO.File]::ReadAllBytes($p)) |
    Select-String -Pattern 'nodejs\.org/download/release/v[\d.]+' -AllMatches |
    ForEach-Object { $_.Matches.Value } | Sort-Object -Unique
  ```
- [ ] 測定値を記録（macOS 実測値は **v24.18.0** / Claude Desktop 1.25927.0）

> ℹ️ **8-1 と 8-3 が pass していれば Node 要件は満たされている**（アプリ自身の照合＋実際に起動している事実）。8-4 は docs に書く**具体的なバージョン数値**を得るための作業で、数値が取れなくてもリリースは止めない。

### 8-5 Windows キーチェーン（`cmdkey` / Credential Manager）

- [ ] 認証情報が保存されたことを確認（ターゲット名は `<service>:<account>` 形式）
  ```powershell
  cmdkey /list | findstr /i "aiftp"
  ```
  → `aiftp:dogfood-v013-win-production` の形で出れば OK
- [ ] 読み出しは 8-3 の `credential: pass` と 8-6 の push 成功で確認できる（読み出しは PowerShell + Win32 `CredRead` 経由）
- [ ] ⚠️ **PowerShell の初回起動が遅いと読み出しが詰まる既知リスク**（Windows CI で `Add-Type` の C# 実行時コンパイルが 5 秒既定を超えた前例あり）。体感で待たされたら**所要時間をメモ**する

### 8-6 dry-run → 実 push（パス区切り混在の検証）

- [ ] 「テストサイトに何が上がるか確認して」→ **宛先バナー**にサイト名・`sv73.star.ne.jp`・`/aiftp-test-win` が正しく出るか目視
- [ ] アップロード予定ファイル数が 8-0 で置いた数と一致するか
- [ ] 「反映して」→ **6文字のチャレンジコード**が出ることを確認
- [ ] チャレンジコード + 合言葉で承認 → **push 成功**
  - 🔴 **バックスラッシュ区切りで入力したフォルダの中身が、リモートで正しいスラッシュ区切りに変換されているか**をリモート一覧で確認（この Step の主目的）
  - サブフォルダを1つ作って中にファイルを置くと、区切り変換の検証がより確実
- [ ] ブラウザで `https://glocalworks.co.jp/aiftp-test-win/` を開いて反映を目視
- [ ] ⚠️ **バックアップ鍵の初期化エラーが出るか**を記録（発見6。Windows には repo が無いので `aiftp backup init` を実行できない＝受講者と同じ状況。ここでどう転ぶかが v0.13.1 の設計材料になる）

### 8-7 後始末

- [ ] リモート `/glocalworks.co.jp/public_html/aiftp-test-win/` を削除（aiftp の push では消えない＝Safe by default。FTPS で直接削除する）
- [ ] Windows 側の Credential Manager から `aiftp:dogfood-v013-win-*` を削除（任意）
- [ ] 🔴 **合言葉のローテーション**（v0.13.0 リリース後でよい）

### Step 8 実施結果（2026-08-15・Windows 11 / Claude Desktop 1.30096.1）— **GO**

**確認できたこと**

| 項目 | 結果 |
|---|---|
| 同梱 Node で起動 | ✅ ログに `isUsingBuiltInNodeForMcp is true and built-in node is compatible`。別途入っている Node は使われない |
| パス区切り変換 | ✅ `assets\style.css` → リモート `assets/style.css`。ディレクトリも自動作成 |
| Safe by default | ✅ `plannedDeletes: []`・本番ファイル無傷 |
| Windows 資格情報マネージャー | ✅ 読み書きとも動作（`cmdkey` 書き込み / `CredRead` 読み出し） |
| 二段確認ゲート | ✅ チャレンジコード＋合言葉で機能 |
| 暗号化バックアップ | ✅ 自動取得され rollback 可能 |
| 未署名インストール | ✅ 個人アカウントで可能 |

**見つかった不具合4件**

1. 🔴 **`.mcpb` のダブルクリックが効かない** — Claude のショートカットへドラッグが必要。→ `docs/desktop-extension.md` に反映済み
2. 🔴 **`display_name` のスラッシュで有効化時に `path escape` エラー** — 「拡張機能サーバーに接続できません」と表示される（リトライで接続はする）。→ **v0.13.0 で修正**（`FTP・FTPS` に変更＋回帰テスト追加）。同一機で解消を確認
3. 🟡 **設定変更が Claude Desktop 再起動まで `.aiftp.toml` に反映されない** — `user` 行が古いままなのを実測、再起動後に更新を確認。しかも `setup_status` はその間ずっと6項目 pass を返す → **失敗が FTP 接続の瞬間まで先送りされ、530 として現れる**。→ v0.13.1
4. 🟡 **backup 鍵がブートストラップで作られない** — `aiftp backup init --profile production` が別途必要。Windows でも再現（既知の躓き②を確定）。→ v0.13.1

> **3と4は同じ構造の問題**: `setup_status` が「存在」だけを見て「正しさ」を見ていない。v0.13.1 の中心課題とする。

**躓きポイントの追加（研修手順書へ）**

3. **user 誤記は 530 まで検出されない** — ただし `aiftp_profile_test`（doctor）は `ftp-auth: fail` として正しく検出した。躓き①（host 誤記）と同じく、**push 前に doctor を回す**運用で防げる。

### Step 8 の判定

- **GO**: 8-1 の要件表示が合格 ＋ 8-3 が 6項目 pass ＋ 8-6 の push 成功 → v0.13.0 リリースへ
- **STOP**: 8-1 で要件未達が表示される / 8-3 で `bootstrap` が起動しない → Node 要件の問題。リリースを止めて対処
- **記録して進む**: `cmdkey` の遅延・バックアップ鍵エラー・パス区切りの軽微な表示崩れ → v0.13.1 backlog へ

---

## 判定

- **GO**: Step 1〜7 がすべて期待どおり ＋ Step 8（実施した場合）も pass → v0.13.0 リリースへ
- **STOP**: 未署名の追加警告が出た / Windows の同梱 Node が要件未満 / 合言葉ゲートが素通りする → 該当を修正してから再実施

> **2026-08-09 時点の状況**: Step 1〜5 すべて pass（合言葉ゲート＝最重要項目が実機で機能確認済み）。Step 6/7 は未実施だが自動テスト（destination-changed / acknowledge_production）で担保ありと判断済み。Step 8（Windows）は未実施。インストール時の赤い警告は「未署名の**追加**警告」ではなく標準の未検証警告であり STOP 条件に該当しない。

## 記録すること

- [x] インストールダイアログの実文言（Step 1 に記録済み。赤い警告あり）
- [x] `hint` の日本語が対処に足りたか（研修の躓きポイント）

### 実測で判明した躓きポイント（研修手順書に反映する）

1. **host 誤記は TLS 段階まで検出されない** — `.aiftp.toml` の host を `stars.ne.jp` と誤記→証明書 `*.star.ne.jp` と不一致で push が TLS で失敗。**合言葉承認は通過した後に落ちる**ため原因が分かりにくい。`aiftp_profile_test`（読み取り専用・合言葉不要）を先に回せば push 前に検出できる
2. **`aiftp backup init --profile <name>` はサイト×プロファイル単位で必要** — `setup_status` は FTP 認証情報しか見ないため、backup 鍵未初期化でも pass のまま通過する。ターミナル必須の手順が Desktop 導線に1つだけ残るのは矛盾（→ v0.13.1 候補: bootstrap で自動初期化）

### ドッグフード完了後の残タスク

- [ ] テストページ `https://glocalworks.co.jp/aiftp-test/` の後始末（削除、または公開状態で残すなら `noindex` / Basic 認証）
- [ ] **合言葉のローテーション**（Desktop の会話履歴に平文で複数回残ったため。ローテーションは田中さん自身が実施）
- [ ] 想定外の挙動・UX の引っかかり → v0.13.1 backlog へ
